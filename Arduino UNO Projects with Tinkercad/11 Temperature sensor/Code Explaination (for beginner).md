# 🌡️ TMP36 Temperature Sensor - বিস্তারিত বাংলা টিউটোরিয়াল

![Temperature Sensor](https://img.shields.io/badge/Sensor-TMP36-red?style=for-the-badge)
![Language](https://img.shields.io/badge/ভাষা-বাংলা-success?style=for-the-badge)
![Level](https://img.shields.io/badge/লেভেল-শিক্ষানবিস-green?style=for-the-badge)

---

## 📚 সূচিপত্র
- [প্রজেক্ট পরিচিতি](#-প্রজেক্ট-পরিচিতি)
- [TMP36 কী?](#-tmp36-কী)
- [যন্ত্রপাতি পরিচিতি](#-যন্ত্রপাতি-পরিচিতি)
- [সার্কিট ডায়াগ্রাম](#-সার্কিট-ডায়াগ্রাম)
- [কাজের নীতি](#-কাজের-নীতি)
- [কোড ব্যাখ্যা](#-কোড-ব্যাখ্যা)
- [পদক্ষেপসমূহ](#-পদক্ষেপসমূহ)
- [সমস্যা সমাধান](#-সমস্যা-সমাধান)
- [শিক্ষণীয় বিষয়](#-শিক্ষণীয়-বিষয়)

---

## 🎯 প্রজেক্ট পরিচিতি

এই প্রজেক্টে আমরা **TMP36 analog temperature sensor** ব্যবহার করে তাপমাত্রা মাপব। এই sensor সরাসরি তাপমাত্রাকে voltage-এ রূপান্তরিত করে, যা Arduino পড়ে আমাদের তিনটি unit-এ দেখায়: **Celsius (°C)**, **Fahrenheit (°F)**, এবং **Kelvin (K)**।

### 🎓 এই প্রজেক্ট থেকে শিখব:
- ✅ TMP36 sensor কীভাবে কাজ করে
- ✅ Voltage থেকে তাপমাত্রা হিসাব করা
- ✅ ADC (Analog-to-Digital Converter) ব্যবহার
- ✅ °C, °F, K-এ রূপান্তর
- ✅ Floating-point calculation
- ✅ তাপমাত্রা ভিত্তিক automation

---

## 💡 TMP36 কী?

### সহজ ভাষায়:

**TMP36** হল একটি বিশেষ sensor যা তাপমাত্রা মাপে এবং সেই অনুযায়ী voltage তৈরি করে। এটি খুবই সহজ - শুধু 3টি wire লাগানো লাগে, আর কোনো extra component লাগে না!

```
তাপমাত্রা বাড়লে → Voltage বাড়ে
তাপমাত্রা কমলে → Voltage কমে

সম্পর্ক: একদম সরল রেখার মতো (Linear)!
```

### TMP36 vs অন্যান্য Sensor:

| Sensor | ধরন | Accuracy | Range | সুবিধা |
|--------|-----|----------|-------|--------|
| **TMP36** | Analog | ±2°C | -40°C to +125°C | সহজ, সরাসরি |
| **DHT11** | Digital | ±2°C | 0°C to 50°C | Humidity ও মাপে |
| **DS18B20** | Digital | ±0.5°C | -55°C to +125°C | খুব নির্ভুল |
| **LM35** | Analog | ±0.5°C | -55°C to +150°C | নির্ভুল, ব্যয়বহুল |

### TMP36-এর বৈশিষ্ট্য:

```
Operating Voltage: 2.7V - 5.5V (Arduino 5V perfect!)
Current: < 50μA (খুব কম বিদ্যুৎ খায়)
Temperature Range: -40°C to +125°C
Accuracy: ±1°C (typical), ±2°C (maximum)
Output: 10mV per °C (Linear!)
```

---

## 🧰 যন্ত্রপাতি পরিচিতি

### 1️⃣ **TMP36 Temperature Sensor**

```
TMP36 চেহারা (সামনে থেকে দেখলে, flat side সামনে):
┌─────────────────┐
│    T M P 3 6    │  ◄── লেখা থাকে
│                 │
│  ┌───────────┐  │
│  │Temperature│  │  ◄── ভিতরে sensing chip
│  │  Sensor   │  │
│  └───────────┘  │
│                 │
│   1    2    3   │  ◄── তিনটি pin
└───┼────┼────┼───┘
    │    │    │
   +Vs  Vout GND
   (5V) (A0) (-)
```

#### Pin চেনার উপায় (CRITICAL!):

**⚠️ সতর্কতা:** TMP36-এর flat side (সমতল দিক) অবশ্যই নিজের দিকে রেখে pin দেখতে হবে!

| Pin Number | নাম | কাজ | সংযোগ |
|-----------|-----|-----|--------|
| **Pin 1 (বাঁয়ে)** | +Vs | বিদ্যুৎ সরবরাহ | Arduino 5V |
| **Pin 2 (মাঝে)** | Vout | Signal output | Arduino A0 |
| **Pin 3 (ডানে)** | GND | Ground | Arduino GND |

**মনে রাখার উপায়:**
```
আমার দিকে flat side রেখে দেখলে:
  বাঁয়ে = Power (5V) → Red wire
  মাঝে = Signal (A0) → Yellow wire
  ডানে = Ground (GND) → Black wire
```

#### TMP36-এর কাজের নীতি:

TMP36-এর একটি **সরল সূত্র** আছে:

```
Vout = (Temperature × 10mV) + 500mV

উদাহরণ:
  -40°C → (-40 × 10mV) + 500mV = 100mV (0.1V)
    0°C → (0 × 10mV) + 500mV = 500mV (0.5V)
  +25°C → (25 × 10mV) + 500mV = 750mV (0.75V)
  +50°C → (50 × 10mV) + 500mV = 1000mV (1.0V)
 +100°C → (100 × 10mV) + 500mV = 1500mV (1.5V)

প্রতি 1°C বৃদ্ধিতে 10mV (0.01V) voltage বাড়ে!
```

**কেন 500mV offset?**

```
যদি offset না থাকত:
  0°C = 0V
  -10°C = -0.1V (Negative voltage!)
  
Arduino শুধু 0V to 5V পড়তে পারে, negative voltage নয়!

500mV offset দিলে:
  -40°C = 100mV (positive!)
  0°C = 500mV
  +125°C = 1750mV
  
সব reading positive থাকে! 👍
```

### 2️⃣ **Arduino UNO**

এই প্রজেক্টে Arduino-র কাজ:
1. TMP36-কে 5V বিদ্যুৎ দেওয়া
2. A0 pin দিয়ে voltage পড়া
3. ADC দিয়ে voltage-কে number-এ রূপান্তরিত করা (0-1023)
4. গাণিতিক সূত্র প্রয়োগ করে তাপমাত্রা বের করা
5. Serial Monitor-এ °C, °F, K display করা

---

## 🔌 সার্কিট ডায়াগ্রাম

### সংযোগ তালিকা:

| TMP36 Pin | Arduino Pin | বিস্তারিত |
|-----------|-------------|-----------|
| Pin 1 (বাঁয়ে) | 5V | বিদ্যুৎ সরবরাহ |
| Pin 2 (মাঝে) | A0 | Voltage signal |
| Pin 3 (ডানে) | GND | Ground |

### Circuit Diagram (ASCII):

```
          Arduino UNO
          ┌───────────┐
          │           │
    ┌─────┤ 5V        │
    │     │           │
    │  ┌──┤ A0        │
    │  │  │           │
    │  │ ┌┤ GND       │
    │  │ │└───────────┘
    │  │ │
    │  │ │
┌───┴──┼─┴────┐
│  T M P 3 6  │  ◄── Flat side সামনে
│             │
│  1   2   3  │
└──┼───┼───┼──┘
  +Vs Vout GND

খুবই সহজ: মাত্র 3টি wire!
```

### Breadboard Connection:

```
Breadboard Setup:
┌─────────────────────────────┐
│ + (Red Rail)                │◄── Arduino 5V
│ - (Blue Rail)               │◄── Arduino GND
│                             │
│ Row 10: TMP36 Pin 1 → +Rail │
│ Row 11: TMP36 Pin 2 → A0    │
│ Row 12: TMP36 Pin 3 → -Rail │
└─────────────────────────────┘

⚠️ Flat side সামনে রেখে বসান!
```

### Wire Color Convention:

```
🔴 Red wire:    5V → TMP36 Pin 1
🟡 Yellow wire: A0 → TMP36 Pin 2
⚫ Black wire:  GND → TMP36 Pin 3
```

---

## ⚙️ কাজের নীতি

### ধাপে ধাপে কীভাবে কাজ করে:

```
ধাপ ১: TMP36 তাপমাত্রা মাপে
  Internal silicon chip temperature sense করে
   ↓
ধাপ ২: Voltage তৈরি করে
  Formula: Vout = (Temp × 10mV) + 500mV
  25°C → 750mV output
   ↓
ধাপ ৩: Arduino A0 pin voltage পড়ে
  750mV signal A0 pin-এ আসে
   ↓
ধাপ ৪: ADC conversion
  10-bit ADC: 750mV → Digital value 153
  Formula: (0.75V / 5V) × 1023 = 153
   ↓
ধাপ ৫: Voltage পুনরায় হিসাব
  Digital → Voltage: (153 / 1023) × 5V = 0.748V
   ↓
ধাপ ৬: Temperature হিসাব (Celsius)
  Temp = (Voltage - 0.5) × 100
  Temp = (0.748 - 0.5) × 100 = 24.8°C
   ↓
ধাপ ৭: Fahrenheit রূপান্তর
  °F = (°C × 9/5) + 32
  °F = (24.8 × 1.8) + 32 = 76.64°F
   ↓
ধাপ ৮: Kelvin রূপান্তর
  K = °C + 273.15
  K = 24.8 + 273.15 = 297.95K
   ↓
ধাপ ৯: Serial Monitor-এ দেখানো
  "Temperature: 24.8 C, 76.64 F, 297.95 K"
```

### Arduino ADC (Analog-to-Digital Converter):

Arduino UNO-তে **10-bit ADC** আছে, এর মানে:

```
Input: 0V to 5V (Analog voltage)
Output: 0 to 1023 (Digital number)

Resolution: 5V / 1024 = 4.88mV per step

হিসাব:
  Digital Value = (Voltage / 5V) × 1023

উদাহরণ:
  0V    → 0
  0.5V  → 102   (0°C)
  0.75V → 153   (25°C)
  1.0V  → 205   (50°C)
  5V    → 1023
```

### TMP36 Voltage-Temperature সম্পর্ক:

```
Temperature vs Voltage:

Voltage (V)
1.75├───────────────╱ (125°C)
    │              ╱
1.50│            ╱ (100°C)
    │          ╱
1.25│        ╱ (75°C)
    │      ╱
1.00│    ╱ (50°C)
    │  ╱
0.75│╱ (25°C)
0.50├ (0°C)
0.10├ (-40°C)
    └────────────────────────► Temperature (°C)
   -40   0   25  50  75  100  125

একদম সোজা রেখা! Linear relationship!
প্রতি 10mV বৃদ্ধি = 1°C বৃদ্ধি
```

---

## 💻 কোড ব্যাখ্যা

### সম্পূর্ণ কোড:

```cpp
/*
 * Project: TMP36 Temperature Sensor
 * তাপমাত্রা মাপার প্রজেক্ট
 */

const int sensorPin = A0;

void setup() {
  Serial.begin(9600);
}

void loop() {
  // ADC reading (0-1023)
  int sensorValue = analogRead(sensorPin);
  
  // Voltage হিসাব
  float voltage = sensorValue * (5.0 / 1023.0);
  
  // Celsius হিসাব
  float temperature = (voltage - 0.5) * 100.0;
  
  // Fahrenheit হিসাব
  float fahrenheit = (temperature * 9.0 / 5.0) + 32.0;
  
  // Kelvin হিসাব
  float kelvin = temperature + 273.15;
  
  // Display
  Serial.print("Temperature: ");
  Serial.print(temperature);
  Serial.print(" C, ");
  Serial.print(fahrenheit);
  Serial.print(" F, ");
  Serial.print(kelvin);
  Serial.println(" K");
  
  delay(1000);
}
```

---

### লাইন বাই লাইন ব্যাখ্যা:

#### 1️⃣ **Pin Declaration:**

```cpp
const int sensorPin = A0;
```

**ব্যাখ্যা:**
- `const int` = স্থির integer (পরিবর্তন হবে না)
- `sensorPin` = আমরা নাম দিয়েছি
- `A0` = TMP36-এর Pin 2 এখানে সংযুক্ত

**কেন A0?**
- Arduino-তে 6টি analog pin আছে (A0-A5)
- যেকোনো একটি ব্যবহার করা যায়
- A0 প্রথম sensor-এর জন্য standard

#### 2️⃣ **Setup Function:**

```cpp
void setup() {
  Serial.begin(9600);
}
```

**ব্যাখ্যা:**
- `Serial.begin(9600)` = Computer-এর সাথে যোগাযোগ চালু
- `9600` = Baud rate (প্রতি সেকেন্ডে 9600 bits)
- Setup একবার মাত্র চলে (Arduino চালু হলে)

**কেন pinMode নেই?**
- Analog pin-এ pinMode লাগে না
- Arduino automatically A0-কে analog input হিসেবে ধরে নেয়

#### 3️⃣ **ADC Reading:**

```cpp
int sensorValue = analogRead(sensorPin);
```

**ব্যাখ্যা:**
- `analogRead()` = A0 pin-এর voltage পড়ে
- Return করে: 0-1023 (10-bit number)
- `int` type যথেষ্ট (0-1023 range)

**Typical Values:**

| তাপমাত্রা | Voltage | ADC Value |
|-----------|---------|-----------|
| -40°C | 100mV | 20 |
| 0°C | 500mV | 102 |
| 25°C (room) | 750mV | 153 |
| 37°C (body) | 870mV | 178 |
| 50°C | 1000mV | 205 |
| 100°C | 1500mV | 307 |

#### 4️⃣ **Voltage Calculation:**

```cpp
float voltage = sensorValue * (5.0 / 1023.0);
```

**ব্যাখ্যা:**
- ADC value-কে আবার voltage-এ রূপান্তরিত করা
- Formula: `Voltage = (ADC / 1023) × 5V`

**Step-by-Step হিসাব (ADC = 153):**

```
Step 1: sensorValue = 153
Step 2: 5.0 / 1023.0 = 0.00488V (per step)
Step 3: 153 × 0.00488 = 0.748V
Step 4: voltage = 0.748V

Alternative calculation:
  voltage = (153 / 1023) × 5
  voltage = 0.14955 × 5
  voltage = 0.748V
```

**কেন float?**

```cpp
// Integer division (ভুল!)
int result = 5 / 1023;  // = 0 (truncated!)

// Float division (সঠিক!)
float result = 5.0 / 1023.0;  // = 0.00488V ✓
```

#### 5️⃣ **Temperature Calculation (Celsius):**

```cpp
float temperature = (voltage - 0.5) * 100.0;
```

**ব্যাখ্যা:**
এটি TMP36-এর **inverse formula**!

```
TMP36 Formula:
  Vout = (Temp × 0.01) + 0.5

Solving for Temp:
  Vout - 0.5 = Temp × 0.01
  Temp = (Vout - 0.5) / 0.01
  Temp = (Vout - 0.5) × 100

কেন 0.5 বিয়োগ?
  → 500mV offset সরানোর জন্য

কেন 100 দিয়ে গুণ?
  → 0.01 (10mV per °C) দিয়ে ভাগ করা = 100 দিয়ে গুণ
```

**উদাহরণ হিসাব (voltage = 0.748V):**

```
Step 1: voltage - 0.5
        = 0.748 - 0.5
        = 0.248V

Step 2: × 100
        = 0.248 × 100
        = 24.8

Answer: 24.8°C ✓
```

**বিভিন্ন Temperature Calculation:**

| Voltage | (voltage - 0.5) | × 100 | Result |
|---------|----------------|-------|--------|
| 0.1V | -0.4V | -40 | -40°C |
| 0.5V | 0V | 0 | 0°C |
| 0.75V | 0.25V | 25 | 25°C |
| 1.0V | 0.5V | 50 | 50°C |
| 1.75V | 1.25V | 125 | 125°C |

#### 6️⃣ **Fahrenheit Conversion:**

```cpp
float fahrenheit = (temperature * 9.0 / 5.0) + 32.0;
```

**ব্যাখ্যা:**
Celsius থেকে Fahrenheit-এ রূপান্তর।

```
Formula:
  °F = (°C × 9/5) + 32
  °F = (°C × 1.8) + 32

উদাহরণ (25°C):
  Step 1: 25 × 9/5 = 25 × 1.8 = 45
  Step 2: 45 + 32 = 77
  Answer: 77°F

সাধারণ রূপান্তর:
  0°C = 32°F (হিমাঙ্ক)
  25°C = 77°F (room temperature)
  37°C = 98.6°F (দেহের তাপমাত্রা)
  100°C = 212°F (স্ফুটনাঙ্ক)
```

**কেন 9.0 / 5.0?**

```cpp
// Integer division (ভুল!)
float wrong = 9 / 5;  // = 1 (truncated!)

// Float division (সঠিক!)
float correct = 9.0 / 5.0;  // = 1.8 ✓
```

#### 7️⃣ **Kelvin Conversion:**

```cpp
float kelvin = temperature + 273.15;
```

**ব্যাখ্যা:**
Celsius থেকে Kelvin-এ রূপান্তর (সবচেয়ে সহজ!)

```
Formula:
  K = °C + 273.15

উদাহরণ (25°C):
  K = 25 + 273.15
  K = 298.15K

গুরুত্বপূর্ণ বিন্দু:
  0°C = 273.15K
  Absolute Zero = 0K = -273.15°C
  
Kelvin স্কেলে কোনো negative value নেই!
1K = 1°C (degree size একই)
```

#### 8️⃣ **Serial Output:**

```cpp
Serial.print("Temperature: ");
Serial.print(temperature);
Serial.print(" C, ");
Serial.print(fahrenheit);
Serial.print(" F, ");
Serial.print(kelvin);
Serial.println(" K");
```

**ব্যাখ্যা:**
- `Serial.print()` = টেক্সট print করে (newline নেই)
- `Serial.println()` = টেক্সট print + newline

**Output দেখতে:**
```
Temperature: 24.80 C, 76.64 F, 297.95 K
Temperature: 24.85 C, 76.73 F, 298.00 K
Temperature: 24.78 C, 76.60 F, 297.93 K
```

**Decimal Places Control:**

```cpp
// 2 decimal places দেখাতে
Serial.print(temperature, 2);  // 24.80

// 1 decimal place দেখাতে
Serial.print(temperature, 1);  // 24.8

// Integer only
Serial.print((int)temperature);  // 24
```

#### 9️⃣ **Delay:**

```cpp
delay(1000);
```

**ব্যাখ্যা:**
- `1000` milliseconds = 1 second
- প্রতি 1 সেকেন্ডে একটি reading নেয়

**কেন Delay?**
- তাপমাত্রা ধীরে পরিবর্তন হয় (fast reading দরকার নেই)
- Serial Monitor পড়া সহজ হয়

**Delay Options:**

| Delay (ms) | সময় | Readings/sec |
|-----------|------|--------------|
| 500 | 0.5s | 2 |
| 1000 | 1s | 1 |
| 2000 | 2s | 0.5 |
| 5000 | 5s | 0.2 (data logging) |

---

### Advanced Code Examples:

#### **Temperature Warning System:**

```cpp
const float MAX_TEMP = 30.0;   // সর্বোচ্চ তাপমাত্রা
const float MIN_TEMP = 15.0;   // সর্বনিম্ন তাপমাত্রা

void loop() {
  int sensorValue = analogRead(sensorPin);
  float voltage = sensorValue * (5.0 / 1023.0);
  float temperature = (voltage - 0.5) * 100.0;
  
  Serial.print("Temperature: ");
  Serial.print(temperature);
  Serial.print(" C - ");
  
  if (temperature > MAX_TEMP) {
    Serial.println("⚠ খুব গরম! Fan চালু করুন!");
  } else if (temperature < MIN_TEMP) {
    Serial.println("❄ খুব ঠান্ডা! Heater চালু করুন!");
  } else {
    Serial.println("✓ স্বাভাবিক তাপমাত্রা");
  }
  
  delay(1000);
}
```

#### **LED Indicator:**

```cpp
const int greenLED = 11;   // স্বাভাবিক
const int yellowLED = 12;  // মাঝারি
const int redLED = 13;     // বিপদ

void setup() {
  pinMode(greenLED, OUTPUT);
  pinMode(yellowLED, OUTPUT);
  pinMode(redLED, OUTPUT);
  Serial.begin(9600);
}

void loop() {
  int sensorValue = analogRead(sensorPin);
  float voltage = sensorValue * (5.0 / 1023.0);
  float temperature = (voltage - 0.5) * 100.0;
  
  // সব LED বন্ধ করা
  digitalWrite(greenLED, LOW);
  digitalWrite(yellowLED, LOW);
  digitalWrite(redLED, LOW);
  
  // তাপমাত্রা অনুযায়ী LED জ্বালানো
  if (temperature < 20) {
    digitalWrite(redLED, HIGH);  // ঠান্ডা
    Serial.print("❄ COLD: ");
  } else if (temperature >= 20 && temperature <= 30) {
    digitalWrite(greenLED, HIGH);  // স্বাভাবিক
    Serial.print("✓ NORMAL: ");
  } else {
    digitalWrite(yellowLED, HIGH);  // গরম
    Serial.print("🔥 HOT: ");
  }
  
  Serial.print(temperature);
  Serial.println(" C");
  delay(1000);
}
```

#### **Stable Reading (Moving Average):**

```cpp
// গড় বের করে stable reading
const int numReadings = 10;
float readings[numReadings];
int readIndex = 0;
float total = 0;
float average = 0;

void setup() {
  Serial.begin(9600);
  // Array initialize
  for (int i = 0; i < numReadings; i++) {
    readings[i] = 0;
  }
}

void loop() {
  int sensorValue = analogRead(sensorPin);
  float voltage = sensorValue * (5.0 / 1023.0);
  float temperature = (voltage - 0.5) * 100.0;
  
  // Moving average হিসাব
  total = total - readings[readIndex];
  readings[readIndex] = temperature;
  total = total + readings[readIndex];
  readIndex = (readIndex + 1) % numReadings;
  average = total / numReadings;
  
  Serial.print("Current: ");
  Serial.print(temperature);
  Serial.print(" C, Stable: ");
  Serial.print(average);
  Serial.println(" C");
  
  delay(500);
}
```

---

## 📋 পদক্ষেপসমূহ

### ধাপ ১: TMP36 Pin চেনা

```
⚠️ CRITICAL: Flat side আপনার দিকে রাখুন!

TMP36 (সামনে থেকে):
┌─────────────┐
│  T M P 3 6  │  ◄── লেখা পড়া যাবে
│             │
│  1   2   3  │
└──┼───┼───┼──┘
   │   │   │
  +Vs Vout GND
   
বাঁয়ে = 5V (Red)
মাঝে = A0 (Yellow)
ডানে = GND (Black)
```

### ধাপ ২: Connection করা

**Checklist:**
```
✅ TMP36 flat side সঠিক দিকে?
✅ Pin 1 (বাঁয়ে) → 5V
✅ Pin 2 (মাঝে) → A0
✅ Pin 3 (ডানে) → GND
✅ কোনো short circuit নেই?
```

### ধাপ ৩: Code Upload

1. Arduino IDE খুলুন
2. `temperature_sensor.ino` file load করুন
3. **Tools → Board → Arduino UNO**
4. **Tools → Port → COM[X]** (আপনার port)
5. **Upload** button (→) click করুন
6. "Done uploading" দেখা পর্যন্ত অপেক্ষা করুন

### ধাপ ৪: Serial Monitor

```
Tools → Serial Monitor (বা Ctrl+Shift+M)
Baud rate: 9600 নির্বাচন করুন

Output দেখতে পাবেন:
"Temperature: 24.5 C, 76.1 F, 297.65 K"
```

### ধাপ ৫: Test করা

**Test Procedures:**

```
Test 1: Room Temperature
  আশা: 20-25°C (স্বাভাবিক)
  
Test 2: হাত দিয়ে ধরুন
  আশা: তাপমাত্রা বাড়বে (body heat)
  
Test 3: Ice cube কাছে আনুন
  আশা: তাপমাত্রা কমবে
  
Test 4: Fan দিয়ে বাতাস দিন
  আশা: সামান্য কমবে (air flow)
```

---

## 🔧 সমস্যা সমাধান

### সাধারণ সমস্যা:

| সমস্যা | কারণ | সমাধান |
|--------|------|--------|
| সবসময় -40°C | TMP36 উল্টো | 180° ঘুরিয়ে লাগান |
| সবসময় 125°C | Power/GND short | Connection চেক করুন |
| অস্থির reading | Loose wire | Wire ভালোভাবে লাগান |
| Wrong temperature | ভুল formula | Code verify করুন |
| Serial Monitor blank | Baud rate ভুল | 9600 select করুন |

### TMP36 Test:

```cpp
// TMP36 ঠিক আছে কিনা test
void testTMP36() {
  Serial.println("TMP36 Test শুরু...");
  Serial.println("Room temperature-এ রাখুন");
  delay(3000);
  
  int sensorValue = analogRead(sensorPin);
  float voltage = sensorValue * (5.0 / 1023.0);
  float temperature = (voltage - 0.5) * 100.0;
  
  Serial.print("Reading: ");
  Serial.print(temperature);
  Serial.println(" C");
  
  if (temperature >= 15 && temperature <= 35) {
    Serial.println("✓ TMP36 ঠিক আছে!");
  } else {
    Serial.println("✗ Problem আছে! Connection চেক করুন");
  }
}
```

---

## 🎓 শিক্ষণীয় বিষয়

### যা শিখলাম:

```
✅ TMP36 sensor-এর কাজের নীতি
✅ Voltage-to-temperature conversion
✅ ADC (Analog-to-Digital) conversion
✅ Floating-point calculation
✅ Unit conversion (°C, °F, K)
✅ Mathematical formula implementation
✅ Serial communication
```

### Temperature Scales মনে রাখার কৌশল:

```
Celsius (°C):
  • পানি জমে: 0°C
  • Room temp: 20-25°C
  • দেহের তাপমাত্রা: 37°C
  • পানি ফুটে: 100°C

Fahrenheit (°F):
  • পানি জমে: 32°F
  • Room temp: 68-77°F
  • দেহের তাপমাত্রা: 98.6°F
  • পানি ফুটে: 212°F

Kelvin (K):
  • Absolute zero: 0K
  • পানি জমে: 273.15K
  • Room temp: 293-298K
  • পানি ফুটে: 373.15K
```

### পরবর্তী Project Ideas:

1. **Smart Fan Controller** - তাপমাত্রা অনুযায়ী fan speed
2. **Temperature Logger** - SD card-এ data save
3. **LCD Display** - 16×2 LCD-তে temperature দেখানো
4. **Temperature Alarm** - threshold cross করলে buzzer
5. **Multi-zone Monitoring** - একাধিক TMP36 ব্যবহার
6. **Web Dashboard** - WiFi দিয়ে online monitoring

---

## ❓ প্রশ্নোত্তর

**প্রশ্ন ১: LM35 এর পরিবর্তে TMP36 কেন?**
- উত্তর: TMP36 negative temperature মাপতে পারে (-40°C)। LM35 শুধু 0°C থেকে শুরু।

**প্রশ্ন ২: TMP36 উল্টো লাগালে কী হবে?**
- উত্তর: Sensor গরম হয়ে যাবে এবং নষ্ট হতে পারে! সাবধান!

**প্রশ্ন ৩: Reading কেন fluctuate করে?**
- উত্তর: ADC noise এবং air temperature পরিবর্তন। Moving average ব্যবহার করুন।

**প্রশ্ন ৪: DHT11 এর চেয়ে TMP36 ভালো?**
- উত্তর: TMP36 সহজ এবং দ্রুত, কিন্তু DHT11 humidity ও মাপে।

---

## 👨‍🎓 লেখক

**মো. আখিনুর ইসলাম**  
📚 Energy Science and Engineering (ESE)  
🏫 Khulna University of Engineering & Technology (KUET)

---

## 📄 License

MIT License - Free to use and modify!

---

### ✨ সফলতার Tips:

1. **Flat side সামনে রাখুন** - pin পরিচয়ের জন্য
2. **Room temperature test করুন** - 20-25°C হওয়া উচিত
3. **Formula ভালোভাবে বুঝুন** - (voltage - 0.5) × 100
4. **Serial Monitor baud rate** - অবশ্যই 9600
5. **Patience রাখুন** - তাপমাত্রা ধীরে পরিবর্তন হয়

**শুভকামনা! তাপমাত্রা মাপুন সঠিকভাবে! 🌡️🎉**
