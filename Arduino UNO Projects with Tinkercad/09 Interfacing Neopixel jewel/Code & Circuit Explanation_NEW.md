# 💎 NeoPixel Jewel - Arduino দিয়ে (বিস্তারিত ব্যাখ্যা)

![Arduino Bangla Tutorial](https://img.shields.io/badge/ভাষা-বাংলা-green?style=for-the-badge)
![Difficulty](https://img.shields.io/badge/লেভেল-মধ্যম-yellow?style=for-the-badge)
![NeoPixel](https://img.shields.io/badge/NeoPixel-Jewel-purple?style=for-the-badge)

---

## 📚 সূচিপত্র
- [প্রজেক্ট পরিচিতি](#-প্রজেক্ট-পরিচিতি)
- [NeoPixel Jewel কী?](#-neopixel-jewel-কী)
- [প্রয়োজনীয় যন্ত্রপাতি](#-প্রয়োজনীয়-যন্ত্রপাতি)
- [সার্কিট সংযোগ](#-সার্কিট-সংযোগ)
- [কোড ব্যাখ্যা](#-কোড-ব্যাখ্যা)
- [কীভাবে কাজ করে](#-কীভাবে-কাজ-করে)
- [সমস্যা ও সমাধান](#-সমস্যা-ও-সমাধান)
- [শিক্ষণীয় বিষয়](#-শিক্ষণীয়-বিষয়)

---

## 🎯 প্রজেক্ট পরিচিতি

এই প্রজেক্টে আমরা **7-LED NeoPixel Jewel** ব্যবহার করে সুন্দর বৃত্তাকার RGB animation তৈরি করব। Jewel-এ ৭টি LED আছে - মাঝে ১টি এবং চারদিকে ৬টি - যা একসাথে ফুলের মত দেখায়।

### এই প্রজেক্ট থেকে শিখবেন:
- ✅ Circular LED layout নিয়ন্ত্রণ
- ✅ ৭টি LED আলাদাভাবে program করা
- ✅ Radial (বৃত্তাকার) animation তৈরি
- ✅ Sequential color patterns
- ✅ Wearable projects এর ভিত্তি

---

## 🔬 NeoPixel Jewel কী?

### সাধারণ ভাষায়:

**NeoPixel Jewel** হলো ছোট্ট বৃত্তাকার একটি board যেখানে **৭টি RGB LED** আছে। এটি দেখতে হীরার মত (jewel)!

### LED গুলো কীভাবে সাজানো:

```
NeoPixel Jewel (উপর থেকে দেখলে)
┌──────────────────────────┐
│        ○  LED 1          │
│    ○         ○           │
│ LED 6  ●   LED 2         │  ● = মাঝের LED (0)
│    ○   0    ○            │  ○ = বাইরের LEDs (1-6)
│ LED 5     LED 3          │
│        ○  LED 4          │
└──────────────────────────┘

মোট ৭টি LED:
  • 1টি মাঝখানে (index 0)
  • 6টি চারদিকে (indices 1-6)
  • Hexagonal (ষড়ভুজ) pattern
```

### NeoPixel Jewel vs Strip:

| বৈশিষ্ট্য | NeoPixel Strip | NeoPixel Jewel |
|----------|---------------|----------------|
| আকৃতি | সরল রেখা | বৃত্তাকার |
| LED সংখ্যা | বিভিন্ন (4, 8, 30...) | 7টি (স্থির) |
| সাইজ | লম্বা | 23.2mm ব্যাস |
| Mounting | Adhesive | 4টি mounting holes |
| ব্যবহার | Linear effects | Radial/circular effects |

### WS2812B Specifications:

| বৈশিষ্ট্য | মান |
|----------|-----|
| ভোল্টেজ | 5V DC (4-7V চলে) |
| কারেন্ট প্রতি LED | 60mA (সাদা, সর্বোচ্চ) |
| সাধারণ কারেন্ট | ~40mA (সাদা, typical) |
| Data Protocol | 800 kHz one-wire |
| Color Depth | 24-bit RGB |
| মোট রঙ | 16,777,216 |
| PWM Frequency | 400 Hz |

### Jewel এর পিন:

| পিন | কাজ | ব্যাখ্যা |
|-----|-----|---------|
| **IN (DIN)** | Data Input | Arduino থেকে control signal পায় |
| **5V** | পাওয়ার | 5V সাপ্লাই (3.3V চলবে না!) |
| **GND** | গ্রাউন্ড | 0V মাটি |
| **OUT (DOUT)** | Data Output | পরবর্তী device-এ chain করার জন্য |

### LED Index Map:

```
LED নাম্বার (সামনে থেকে দেখলে):

        1
    6       2
       0
    5       3
        4

Addressing উদাহরণ:
  ring.setPixelColor(0, color);  // মাঝের LED
  ring.setPixelColor(1, color);  // উপরের LED
  ring.setPixelColor(2, color);  // উপর-ডান
  ring.setPixelColor(3, color);  // নিচ-ডান
  ring.setPixelColor(4, color);  // নিচের LED
  ring.setPixelColor(5, color);  // নিচ-বাম
  ring.setPixelColor(6, color);  // উপর-বাম
```

### Physical আকার:

```
NeoPixel Jewel মাপ:
  ব্যাস: 23.2mm (প্রায় এক ইঞ্চি)
  LED spacing: 60° apart (ষড়ভুজ)
  Mounting holes: 4 × Ø2mm
  PCB thickness: 1.2mm
  Weight: ~1.5g (খুবই হালকা!)
```

### বিদ্যুৎ খরচ:

```
7-LED Jewel Power হিসাব:

একটি LED:
  শুধু লাল:   ~20mA @ 255
  শুধু সবুজ:  ~20mA @ 255
  শুধু নীল:   ~20mA @ 255
  সাদা (max): ~60mA @ (255,255,255)
  সাদা (avg): ~40mA (সাধারণত)

পুরো Jewel (7 LEDs):
  সব সাদা (max): 7 × 60mA = 420mA
  সব সাদা (avg): 7 × 40mA = 280mA
  মিশ্র রঙ: 150-250mA (typical)

Arduino 5V Pin: ~400-500mA দেয়
→ <80% brightness-এ safe
→ Full brightness-এ external power ভালো

সুপারিশ:
  Low brightness (<50%): Arduino 5V ঠিক আছে
  High brightness (>50%): External 5V 1A
  একাধিক jewels: 5V 2A+ supply
```

---

## 🧰 প্রয়োজনীয় যন্ত্রপাতি

| যন্ত্রের নাম | সংখ্যা | বিবরণ |
|-------------|--------|-------|
| Arduino UNO | ১টি | ATmega328P মাইক্রোকন্ট্রোলার |
| NeoPixel Jewel | ১টি | 7 LEDs (WS2812B) - Adafruit #2226 |
| ব্রেডবোর্ড | ১টি | সংযোগের জন্য |
| জাম্পার তার | ৩টি | Male-to-Male |
| External Power | ১টি (ঐচ্ছিক) | 5V 1A (>50% brightness জন্য) |
| Capacitor | ১টি (ঐচ্ছিক) | 1000μF (power smoothing) |
| Resistor | ১টি (ঐচ্ছিক) | 470Ω (data সুরক্ষা) |

### 💰 আনুমানিক খরচ: ৮০০-১২০০ টাকা

---

## 🔌 সার্কিট সংযোগ

### তার সংযোগ টেবিল:

| NeoPixel Jewel | Arduino পিন | ব্যাখ্যা |
|----------------|-------------|---------|
| IN (DIN) | D6 | Control signal input |
| 5V | 5V | বিদ্যুৎ সরবরাহ |
| GND | GND | গ্রাউন্ড (মাটি) |
| OUT (DOUT) | সংযুক্ত নয় | পরবর্তী device chain করতে |

### সার্কিট ডায়াগ্রাম:

```
Arduino UNO                    NeoPixel Jewel (7 LEDs)
┌─────────────┐               ┌────────────────────┐
│             │               │      1             │
│   5V  ●─────┼───────────────┤   6   0   2        │
│             │               │      5   3         │
│  GND  ●─────┼───────────────┤        4           │
│             │               │                    │
│   D6  ●─────┼───────────────┤ IN 5V GND OUT      │
│             │               │  │  │  │           │
└─────────────┘               └──┼──┼──┼───────────┘
                                DIN 5V GND

D6 sequential color data পাঠায়
মাঝের LED (0) + 6টি বাইরের LEDs (1-6)
```

### উন্নত সার্কিট (সুপারিশ):

```
Data Line সুরক্ষা:
  Arduino D6 → 470Ω resistor → Jewel IN
  (Voltage spike থেকে বাঁচায়)

Power Smoothing:
  5V Supply ─┬─ 1000μF capacitor ─→ Jewel 5V
             │
            GND (common)
  (LED পরিবর্তনে brownout রোধ করে)

একাধিক Jewel Chaining:
  Jewel 1 OUT → Jewel 2 IN → Jewel 3 IN → ...
  (সবার common 5V এবং GND)
```

### ⚠️ গুরুত্বপূর্ণ সতর্কতা:

```
সংযোগ নিয়ম:
┌─────────────────────────────────┐
│ ১. প্রথমে GND সংযুক্ত করুন      │
│ ২. তারপর 5V সংযুক্ত করুন       │
│ ৩. সবশেষে Data wire (IN)       │
│                                 │
│ Data wire ছোট রাখুন (<6 inch)  │
│ Mounting holes ব্যবহার করুন    │
│ Common ground জরুরি!            │
└─────────────────────────────────┘
```

---

## 💻 কোড ব্যাখ্যা

### সম্পূর্ণ কোড:

```cpp
/*
 * প্রজেক্ট: NeoPixel Jewel (7 LEDs) নিয়ন্ত্রণ
 * লেখক: মোঃ আখিনুর ইসলাম
 * বিবরণ: 7-LED NeoPixel Jewel-এ sequential RGB animation
 */

#include <Adafruit_NeoPixel.h>

#define PIN 6
#define NUMPIXELS 7

Adafruit_NeoPixel ring(NUMPIXELS, PIN, NEO_GRB + NEO_KHZ800);

void setup() {
  ring.begin();
  ring.show(); // সব প্রথমে বন্ধ
}

void loop() {
  // লাল রঙ cycle
  for (int i = 0; i < NUMPIXELS; i++) {
    ring.setPixelColor(i, ring.Color(255, 0, 0));
    ring.show();
    delay(150);
    ring.setPixelColor(i, 0);
  }

  // সবুজ রঙ cycle
  for (int i = 0; i < NUMPIXELS; i++) {
    ring.setPixelColor(i, ring.Color(0, 255, 0));
    ring.show();
    delay(150);
    ring.setPixelColor(i, 0);
  }

  // নীল রঙ cycle
  for (int i = 0; i < NUMPIXELS; i++) {
    ring.setPixelColor(i, ring.Color(0, 0, 255));
    ring.show();
    delay(150);
    ring.setPixelColor(i, 0);
  }
}
```

---

### 📖 লাইন-বাই-লাইন ব্যাখ্যা:

#### ১. Library যুক্ত করা

```cpp
#include <Adafruit_NeoPixel.h>
```

**কী করে?**
- Adafruit-এর NeoPixel library include করে
- WS2812B control এর জটিল timing manage করে
- High-level functions প্রদান করে

---

#### ২. Constant সংজ্ঞা

```cpp
#define PIN 6
#define NUMPIXELS 7
```

**ব্যাখ্যা:**

| Constant | মান | কাজ |
|----------|-----|-----|
| `PIN` | 6 | Data pin (D6) |
| `NUMPIXELS` | 7 | NeoPixel Jewel-এ 7টি LED (1 center + 6 outer) |

**কেন 7?**
- Jewel-এর fixed design
- Strip-এর মত variable নয়

---

#### ৩. Ring Object তৈরি

```cpp
Adafruit_NeoPixel ring(NUMPIXELS, PIN, NEO_GRB + NEO_KHZ800);
```

**Constructor Parameters:**

| Parameter | মান | অর্থ |
|-----------|-----|------|
| `NUMPIXELS` | 7 | মোট LED সংখ্যা |
| `PIN` | 6 | Data output pin |
| `NEO_GRB` | Byte order | Green-Red-Blue (WS2812B standard) |
| `NEO_KHZ800` | 800 kHz | WS2812B protocol গতি |

**কেন "ring"?**
- Variable নাম বৃত্তাকার layout reflect করে
- "jewel", "circle", "pixels" নামও দিতে পারতেন
- Code পড়তে সহজ করে

---

#### ৪. Setup ফাংশন

```cpp
void setup() {
  ring.begin();
  ring.show();
}
```

**Initialization ধাপ:**

| Function | কাজ |
|----------|-----|
| `ring.begin()` | Pin configure করে, memory buffer allocate করে |
| `ring.show()` | সব LED পরিষ্কার করে (black/OFF সেট করে) |

**কেন দুইবার?**
- `begin()` শুরু করে
- `show()` নিশ্চিত করে LEDs OFF আছে

---

#### ৫. লাল রঙ Cycle

```cpp
for (int i = 0; i < NUMPIXELS; i++) {
  ring.setPixelColor(i, ring.Color(255, 0, 0));
  ring.show();
  delay(150);
  ring.setPixelColor(i, 0);
}
```

**ধাপে ধাপে কী হচ্ছে:**

| i | LED অবস্থান | সময় (ms) | রঙ |
|---|-------------|-----------|-----|
| 0 | মাঝে | 0 | লাল |
| 1 | উপরে | 150 | লাল |
| 2 | উপর-ডান | 300 | লাল |
| 3 | নিচ-ডান | 450 | লাল |
| 4 | নিচে | 600 | লাল |
| 5 | নিচ-বাম | 750 | লাল |
| 6 | উপর-বাম | 900 | লাল |

**মোট লাল cycle:** 1050ms (1.05 সেকেন্ড)

**Visual Pattern:**
```
         1           
     6       2   
        0        ← মাঝের LED প্রথমে জ্বলে
     5       3   
         4       
```

---

#### ৬. `setPixelColor()` ফাংশন

```cpp
ring.setPixelColor(i, ring.Color(255, 0, 0));
```

**দুইটি Form:**

```cpp
// Form 1: Packed color (32-bit)
ring.setPixelColor(index, 32bit_color);

// Form 2: আলাদা R,G,B
ring.setPixelColor(index, red, green, blue);
```

**গুরুত্বপূর্ণ:** এটি শুধু memory buffer update করে, LED এখনও update হয় না!

---

#### ৭. `Color()` ফাংশন

```cpp
ring.Color(red, green, blue);
```

**সাধারণ রঙ:**

| রঙ | R | G | B | Emoji |
|-----|---|---|---|-------|
| লাল | 255 | 0 | 0 | 🔴 |
| সবুজ | 0 | 255 | 0 | 🟢 |
| নীল | 0 | 0 | 255 | 🔵 |
| হলুদ | 255 | 255 | 0 | 🟡 |
| সায়ান | 0 | 255 | 255 | 🩵 |
| ম্যাজেন্টা | 255 | 0 | 255 | 🟣 |
| সাদা | 255 | 255 | 255 | ⚪ |
| কমলা | 255 | 165 | 0 | 🟠 |
| বেগুনি | 128 | 0 | 128 | 🟣 |
| গোলাপি | 255 | 192 | 203 | 🩷 |

---

#### ৮. `show()` ফাংশন

```cpp
ring.show();
```

**Critical Function:**
- Buffer LEDs-এ transmit করে
- 7 LEDs-এর জন্য ~210μs লাগে
- সব LED একসাথে update হয়
- `setPixelColor()` এর পর অবশ্যই call করতে হবে!

**মনে রাখার কৌশল:**
```
setPixelColor() = প্রস্তুতি (রান্না করা)
show() = পরিবেশন (খাবার টেবিলে দেওয়া)

রান্না করলেই খাওয়া যায় না, পরিবেশন করতে হয়!
```

---

#### ৯. Animation Delay

```cpp
delay(150);
```

**Timing Options:**

| Delay (ms) | Effect | গতি |
|-----------|--------|------|
| 50 | খুব দ্রুত, follow করা কঠিন | Rapid |
| 100 | দ্রুত, smooth | Quick |
| 150 | মাঝারি (default) | Balanced |
| 300 | ধীর, পরিষ্কার | Leisurely |
| 500 | খুব ধীর, deliberate | Slow-mo |

**আপনার পছন্দ অনুযায়ী পরিবর্তন করতে পারেন!**

---

#### 🔟 LED বন্ধ করা

```cpp
ring.setPixelColor(i, 0);
```

**বন্ধ করার বিভিন্ন উপায়:**
```cpp
ring.setPixelColor(i, 0);                    // Shorthand
ring.setPixelColor(i, ring.Color(0, 0, 0));  // Explicit
ring.setPixelColor(i, 0, 0, 0);              // আলাদা R,G,B
```

সব একই কাজ করে - LED কালো/বন্ধ করে দেয়।

---

### অতিরিক্ত Useful Functions:

```cpp
// Global উজ্জ্বলতা (0-255)
ring.setBrightness(100);  // ~39% উজ্জ্বলতা

// Pixel রঙ পড়া
uint32_t color = ring.getPixelColor(0);

// সব pixels পরিষ্কার
ring.clear();  // সব কালো করে
ring.show();   // অবশ্যই call করতে হবে

// LED সংখ্যা জানা
uint16_t count = ring.numPixels();  // Return 7

// সব একই রঙে fill
ring.fill(ring.Color(255, 0, 0), 0, 7);  // সব লাল
ring.show();

// Rainbow effect (advanced)
void rainbow(uint8_t wait) {
  for(long hue = 0; hue < 65536; hue += 256) {
    for(int i=0; i<ring.numPixels(); i++) {
      int pixelHue = hue + (i * 65536L / ring.numPixels());
      ring.setPixelColor(i, ring.gamma32(ring.ColorHSV(pixelHue)));
    }
    ring.show();
    delay(wait);
  }
}
```

---

## ⚙️ কীভাবে কাজ করে

### Animation Sequence:

```
সময়:      0s      1.05s   2.1s    Loop
Cycle:     লাল  →  সবুজ  → নীল  → লাল...

LED Animation Pattern (Sequential):

Iteration 0: LED 0 লাল ON  → OFF
Iteration 1: LED 1 লাল ON  → OFF
Iteration 2: LED 2 লাল ON  → OFF
...
Iteration 6: LED 6 লাল ON  → OFF

মোট লাল cycle: 7 × 150ms = 1050ms
মোট সবুজ cycle: 1050ms
মোট নীল cycle: 1050ms
সম্পূর্ণ loop: 3150ms (3.15 সেকেন্ড)

Visual Pattern:
         1           1           1
     6       2   6   ●   2   6       2
        0           0           0
     5       3   5       3   5       3
         4           4           4
       লাল         সবুজ         নীল
```

### Circular Animation সম্ভাবনা:

```
Chase Effect (ঘড়ির কাঁটা অনুযায়ী):
  LED 1 → 2 → 3 → 4 → 5 → 6 → repeat

Radial Burst (মাঝ থেকে বাইরে):
  Center (0) ON
  সব outer (1-6) একসাথে ON
  Center OFF, outer OFF

Opposite Pair (বিপরীত জোড়া):
  LED 1 & 4 (বিপরীত দিক)
  LED 2 & 5
  LED 3 & 6
```

---

## 🔧 সমস্যা ও সমাধান

### সাধারণ সমস্যা এবং সমাধান:

| সমস্যা | সম্ভাব্য কারণ | সমাধান |
|--------|--------------|---------|
| কোনো LED জ্বলছে না | ভুল wiring | চেক করুন: IN=D6, 5V=5V, GND=GND |
| শুধু মাঝের LED কাজ করে | বাইরের LEDs নষ্ট | প্রতিটি LED আলাদাভাবে test করুন |
| Random flickering | IN connection loose | Data wire শক্ত করুন, 470Ω resistor যোগ করুন |
| আলো কম/ভুল রঙ | যথেষ্ট পাওয়ার নেই | External 5V supply ব্যবহার করুন |
| Library পাওয়া যাচ্ছে না | Install হয়নি | Library Manager থেকে "Adafruit NeoPixel" install |
| ভুল color order | Incorrect flag | NEO_RGB চেষ্টা করুন NEO_GRB এর বদলে |
| একটা LED আটকে আছে | LED chain-এ failed | Jewel replace করুন বা code-এ skip |

---

### Individual LED Test করা:

```cpp
// প্রতিটি LED test করুন:
void testLEDs() {
  for (int i = 0; i < 7; i++) {
    ring.clear();
    ring.setPixelColor(i, ring.Color(255, 255, 255));  // সাদা
    ring.show();
    Serial.print("LED ");
    Serial.print(i);
    Serial.println(" ON");
    delay(1000);
  }
}
```

### Debugging কোড:

সমস্যা খুঁজতে এই কোড ব্যবহার করুন:

```cpp
#include <Adafruit_NeoPixel.h>

#define PIN 6
#define NUMPIXELS 7

Adafruit_NeoPixel ring(NUMPIXELS, PIN, NEO_GRB + NEO_KHZ800);

void setup() {
  Serial.begin(9600);
  ring.begin();
  ring.setBrightness(50);  // Power কম লাগবে
  ring.show();
  Serial.println("NeoPixel Jewel initialized");
  Serial.println("LED positions:");
  Serial.println("  0 = মাঝে");
  Serial.println("  1-6 = বাইরে (উপর থেকে clockwise)");
}

void loop() {
  for (int i = 0; i < NUMPIXELS; i++) {
    Serial.print("LED ");
    Serial.print(i);
    Serial.print(" (");
    Serial.print(i == 0 ? "মাঝে" : "বাইরে");
    Serial.println(") লাল");
    
    ring.setPixelColor(i, ring.Color(255, 0, 0));
    ring.show();
    delay(500);
    
    ring.setPixelColor(i, ring.Color(0, 0, 0));
    ring.show();
    delay(100);
  }
}
```

---

## 🎓 শিক্ষণীয় বিষয়

### এই প্রজেক্ট থেকে যা শিখলাম:

| ধারণা | ব্যাখ্যা | বাস্তব ব্যবহার |
|------|---------|----------------|
| **Circular LED Arrays** | বৃত্তাকার/symmetric light patterns | Wearables, decorations, indicators |
| **Addressable RGB** | Individual LED control | যেকোনো multi-LED display |
| **Sequential Animation** | Loop-based pattern তৈরি | Visual effects, status indicators |
| **WS2812B Protocol** | 800 kHz one-wire communication | NeoPixel, addressable LED strips |
| **Power Budgeting** | LED array-এর জন্য current হিসাব | যেকোনো power-sensitive project |

---

### 🚀 এই প্রজেক্ট বাড়ানোর আইডিয়া:

#### ১. **Radial Burst Effect**
```cpp
void radialBurst() {
  // মাঝের LED জ্বালান
  ring.setPixelColor(0, ring.Color(255, 255, 255));
  ring.show();
  delay(200);
  
  // বাইরের সব LED একসাথে
  for(int i=1; i<7; i++) {
    ring.setPixelColor(i, ring.Color(255, 255, 255));
  }
  ring.show();
  delay(500);
  
  // সব বন্ধ
  ring.clear();
  ring.show();
}
```

#### ২. **Color Wheel (Rainbow)**
- Continuous rainbow rotation
- প্রতিটি LED ভিন্ন রঙ

#### ৩. **Breathing Effect**
```cpp
void breathe() {
  for(int b=0; b<255; b++) {
    ring.fill(ring.Color(b, 0, 0), 0, 7);
    ring.show();
    delay(10);
  }
  for(int b=255; b>=0; b--) {
    ring.fill(ring.Color(b, 0, 0), 0, 7);
    ring.show();
    delay(10);
  }
}
```

#### ৪. **Opposite Pairs**
```cpp
void oppositePairs() {
  ring.setPixelColor(1, ring.Color(255, 0, 0));  // Top
  ring.setPixelColor(4, ring.Color(255, 0, 0));  // Bottom
  ring.show();
  delay(500);
}
```

#### ৫. **Music Reactive**
- Sound sensor যোগ করুন
- Beat অনুযায়ী pulses

---

### 🌟 বাস্তব জীবনে ব্যবহার:

- 💍 **Wearable Jewelry** - LED earrings, pendants, bracelets
- 🎨 **Art Installations** - Interactive light sculptures
- 🎮 **Gaming Props** - Glowing power-ups, status indicators
- 🤖 **Robot Eyes** - Animated robot facial expressions
- 📱 **Smart Accessories** - Notification indicators
- 🎄 **Holiday Ornaments** - Animated Christmas decorations
- 🏆 **Award Trophies** - Dynamic LED centers

---

## 📚 আরও শিখুন:

### প্রশ্ন-উত্তর:

**প্রশ্ন ১:** NeoPixel Jewel এবং Strip-এর পার্থক্য কী?  
**উত্তর:** Jewel বৃত্তাকার (7 LEDs fixed), Strip সরল রেখা (variable LEDs)। Jewel-এ radial effects সহজ, Strip-এ linear effects।

**প্রশ্ন ২:** মাঝের LED কেন 0 index?  
**উত্তর:** Data প্রথম মাঝের LED-তে আসে, তারপর বাইরের LED-গুলোতে। তাই indexing 0 থেকে শুরু center-এ।

**প্রশ্ন ৩:** একাধিক Jewel একসাথে চালানো যায়?  
**উত্তর:** হ্যাঁ! Jewel 1 OUT → Jewel 2 IN চেইন করুন। NUMPIXELS বাড়িয়ে দিন (7+7=14)।

**প্রশ্ন ৪:** Jewel ওজনে কত?  
**উত্তর:** মাত্র ~1.5g। Wearable projects-এর জন্য perfect!

---

## ✅ Checklist

### প্রজেক্ট শুরুর আগে:

- [ ] Adafruit NeoPixel library install করেছি
- [ ] Jewel-এর pinout চিহ্নিত করেছি (IN, 5V, GND)
- [ ] LED indexing বুঝেছি (0=মাঝে, 1-6=বাইরে)
- [ ] Power requirement হিসাব করেছি

### সংযোগের সময়:

- [ ] প্রথমে GND সংযুক্ত করেছি
- [ ] IN → D6
- [ ] 5V → 5V
- [ ] GND → GND (common ground)
- [ ] Mounting holes ব্যবহার করেছি (stability)

### Code upload এর পর:

- [ ] LEDs sequential ভাবে জ্বলছে (0→1→2→3→4→5→6)
- [ ] লাল → সবুজ → নীল cycle হচ্ছে
- [ ] মাঝের LED প্রথমে জ্বলছে
- [ ] রঙ সঠিক দেখাচ্ছে

---

## 👨‍🎓 লেখক

**মোঃ আখিনুর ইসলাম**  
📚 Energy Science and Engineering (ESE)  
🏫 Khulna University of Engineering & Technology (KUET)  
🌐 [GitHub Profile](https://github.com/Akhinoor14)

---

## 🎯 Challenge Levels

### 🟢 Beginner:
- [ ] শুধু মাঝের LED বিভিন্ন রঙে জ্বালান
- [ ] বাইরের LEDs একসাথে একই রঙে

### 🟡 Intermediate:
- [ ] Clockwise chase effect তৈরি করুন
- [ ] Opposite pairs একসাথে জ্বালান (1&4, 2&5, 3&6)

### 🔴 Advanced:
- [ ] Rainbow wheel animation
- [ ] Music reactive jewel (sound sensor)
- [ ] একাধিক jewels synchronize করুন

---

### 🔗 সম্পর্কিত প্রজেক্ট:

1. [NeoPixel Strip](../08%20Interfacing%20Neopixel%20strip%204/)
2. [RGB LED Control](../05%20RGB%20LED%20Control%20with%20PWM/)
3. [LED Pattern](../01%20LED%20Pattern/)

---

**শুভ কোডিং! 🎉**  
**NeoPixel Jewel দিয়ে সুন্দর কিছু বানান! 💎**
