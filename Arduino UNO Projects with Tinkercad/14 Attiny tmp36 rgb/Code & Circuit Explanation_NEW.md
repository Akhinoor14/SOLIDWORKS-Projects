# 🌡️ ATtiny85 + TMP36 + RGB LED - বিস্তারিত বাংলা টিউটোরিয়াল

![ATtiny85](https://img.shields.io/badge/চিপ-ATtiny85-blue?style=for-the-badge)
![TMP36](https://img.shields.io/badge/সেন্সর-TMP36-green?style=for-the-badge)
![RGB](https://img.shields.io/badge/আউটপুট-RGB%20LED-red?style=for-the-badge)
![Language](https://img.shields.io/badge/ভাষা-বাংলা-success?style=for-the-badge)
![Level](https://img.shields.io/badge/লেভেল-মধ্যম-yellow?style=for-the-badge)

---

## 📚 সূচিপত্র
- [প্রজেক্ট পরিচিতি](#-প্রজেক্ট-পরিচিতি)
- [ATtiny85 কী?](#-attiny85-কী)
- [যন্ত্রপাতি পরিচিতি](#-যন্ত্রপাতি-পরিচিতি)
- [সার্কিট ডায়াগ্রাম](#-সার্কিট-ডায়াগ্রাম)
- [তাপমাত্রা জোন](#-তাপমাত্রা-জোন)
- [কাজের নীতি](#-কাজের-নীতি)
- [কোড ব্যাখ্যা](#-কোড-ব্যাখ্যা)
- [পদক্ষেপসমূহ](#-পদক্ষেপসমূহ)
- [সমস্যা সমাধান](#-সমস্যা-সমাধান)
- [শিক্ষণীয় বিষয়](#-শিক্ষণীয়-বিষয়)

---

## 🎯 প্রজেক্ট পরিচিতি

এই প্রজেক্টে আমরা **ATtiny85 microcontroller**, **TMP36 temperature sensor**, এবং **RGB LED** দিয়ে একটি **portable temperature indicator** তৈরি করব। এটি **5টি ভিন্ন রঙ** দিয়ে তাপমাত্রার মাত্রা দেখাবে। সবচেয়ে মজার বিষয় - এটি **3V coin cell battery**-তে চলবে!

### 🌟 কেন এই প্রজেক্ট বিশেষ?

```
✅ Portable - হাতে নিয়ে যেকোনো জায়গায় নেওয়া যায়
✅ Battery-powered - কোনো wire লাগে না
✅ Compact - খুবই ছোট (coin size!)
✅ Visual - রঙ দেখেই তাপমাত্রা বুঝা যায়
✅ Low-power - 10+ ঘণ্টা চলবে
✅ Simple - মাত্র 7টি component!
```

### ব্যবহার:

```
🏠 বাসায়: ঘরের তাপমাত্রা check করা
🧊 ফ্রিজে: সঠিক ঠান্ডা আছে কিনা দেখা
🩺 শরীরে: জ্বর আছে কিনা check করা
🌡️ পরিবেশে: বাইরের temperature monitor করা
💻 কম্পিউটারে: CPU overheating check করা
```

---

## 🤔 ATtiny85 কী?

### Arduino UNO vs ATtiny85:

```
Arduino UNO:
  • বড় board (28 pins)
  • অনেক memory
  • Complex project-এর জন্য
  • Power consumption বেশি
  • দাম ~₹1500

ATtiny85:
  • খুবই ছোট (8 pins)
  • Basic memory
  • Simple project-এর জন্য
  • Power consumption কম ✅
  • দাম ~₹50 ✅

এই project-এর জন্য ATtiny85 perfect!
```

### ATtiny85 চেহারা:

```
ATtiny85 DIP-8 Package:
        ┌────────┐
  RESET │1  ●  8│ VCC (+3V)
   PB3  │2     7│ PB2 (A1) ← TMP36 সেন্সর
   PB4  │3     6│ PB1 ← সবুজ LED
   GND  │4     5│ PB0 ← লাল LED
        └────────┘
                   ↑
              নীল LED

খুবই ছোট! 8-pin মাত্র।
```

### Pin-এর কাজ:

```
Pin 1: RESET - chip reset করার জন্য
Pin 2: PB3 - ব্যবহার করিনি
Pin 3: PB4 - নীল LED (PWM)
Pin 4: GND - Ground (─)
Pin 5: PB0 - লাল LED (PWM)
Pin 6: PB1 - সবুজ LED (PWM)
Pin 7: PB2/A1 - TMP36 input (Analog)
Pin 8: VCC - Power (+3V)

PWM মানে: LED-এর brightness control করা যায়!
```

---

## 🧰 যন্ত্রপাতি পরিচিতি

### 1️⃣ **ATtiny85 Microcontroller**

```
বৈশিষ্ট্য:
  • 8KB Flash Memory
  • 8 MHz speed
  • 6টি I/O pins
  • 4টি PWM pins
  • 4টি Analog inputs
  • খুবই কম power খায়

দাম: ~₹50-80
```

### 2️⃣ **TMP36 Temperature Sensor**

```
TMP36 - এনালগ তাপমাত্রা সেন্সর
┌─────────────────────────────────┐
│ • Voltage output: 10mV/°C       │
│ • Range: -40°C থেকে +125°C      │
│ • Accuracy: ±2°C                │
│ • Supply: 2.7V - 5.5V           │
│ • Current: <50µA (খুব কম!)      │
└─────────────────────────────────┘

চেহারা (TO-92 Package):
      ┌───────┐
      │ TMP36 │
      │       │
      │ _____ │
      └───────┘
       │  │  │
       1  2  3
       │  │  │
      VCC │ GND
         Vout

Pin 1: VCC (+3V power)
Pin 2: Vout (voltage output → A1)
Pin 3: GND (ground)

দাম: ~₹30-50
```

#### TMP36 কীভাবে কাজ করে?

```
তাপমাত্রা → Voltage Output

Formula:
  Vout = (Temperature × 10mV) + 500mV

উদাহরণ:
  0°C  → 500mV (0.5V)
  25°C → 750mV (0.75V)
  50°C → 1000mV (1.0V)
  
Temperature বাড়লে voltage বাড়ে!
Voltage পড়ে তাপমাত্রা বের করা যায়।
```

### 3️⃣ **Common Cathode RGB LED**

```
RGB LED - 3টি LED একসাথে!
┌────────────────────────────┐
│  R = Red (লাল)             │
│  G = Green (সবুজ)          │
│  B = Blue (নীল)            │
└────────────────────────────┘

Common Cathode মানে:
  • সব LED-এর (─) একসাথে GND-তে
  • প্রতিটি color-এর আলাদা (+) pin

Pinout:
     সবচেয়ে লম্বা pin = Cathode (─)
     
  R    GND    G    B
  │     │     │    │
  ●     ●     ●    ●
        ↑
    সবচেয়ে লম্বা

Color Mixing:
  লাল + সবুজ = হলুদ
  লাল + নীল = ম্যাজেন্টা
  সবুজ + নীল = cyan (আকাশী)
  সব ON = সাদা

দাম: ~₹20-30
```

### 4️⃣ **220Ω Resistor (3টি)**

```
কাজ: LED-কে রক্ষা করা

Why 220Ω?
  LED max current: 20mA
  ATtiny85 output: 3V
  
  R = V / I
  R = 3V / 0.02A = 150Ω
  
  220Ω ব্যবহার করলে safe!
  Current: ~13.6mA (safe zone)

রঙ কোড:
  🔴 Red - ⚫ Black - 🟤 Brown
  2 - 2 - 0 = 220Ω

দাম: ~₹1 each (3টি = ₹3)
```

### 5️⃣ **CR2032 Coin Cell Battery**

```
CR2032 Battery:
┌──────────────────────────┐
│ Voltage: 3V              │
│ Capacity: 220mAh         │
│ Size: 20mm × 3.2mm       │
│ Weight: ~3 grams         │
│ Life: 10+ hours          │
│ Type: Lithium            │
└──────────────────────────┘

(+) উপরে, (─) নিচে
Computer motherboard-এ ব্যবহৃত!

দাম: ~₹30-50
```

### 6️⃣ **DIP Switch (2-position)**

```
DIP Switch:
  ON/OFF করার জন্য

┌────┐
│ ON │ ← Position 1 (Circuit close)
│ OFF│ ← Position 2 (Circuit open)
└────┘

Simple on/off switch!

দাম: ~₹10-20
```

### 7️⃣ **0.1µF Capacitor**

```
Capacitor (100nF):
  কাজ: Voltage stable রাখা

Battery → Capacitor → ATtiny85
         (Voltage ripple filter)

দাম: ~₹2-5
```

### মোট খরচ:

```
┌─────────────────────┬────────┐
│ Component           │ দাম    │
├─────────────────────┼────────┤
│ ATtiny85            │ ₹60    │
│ TMP36               │ ₹40    │
│ RGB LED             │ ₹25    │
│ 220Ω Resistor ×3    │ ₹3     │
│ CR2032 Battery      │ ₹40    │
│ Battery Holder      │ ₹20    │
│ DIP Switch          │ ₹15    │
│ 0.1µF Capacitor     │ ₹3     │
├─────────────────────┼────────┤
│ মোট                 │ ~₹206  │
└─────────────────────┴────────┘

খুবই সস্তা! (~$2.50 USD)
```

---

## 🔌 সার্কিট ডায়াগ্রাম

### সম্পূর্ণ সার্কিট:

```
3V Battery Circuit (Portable):
┌────────────────────────────────────────────────┐
│                                                │
│  ┌──────┐  DIP                                 │
│  │CR2032│   Switch                             │
│  │  3V  │   ┌──┐                               │
│  │220mAh│───┤SW├───┬──────────────┬────────┬───┤
│  └──────┘   └──┘   │              │        │   │
│     Coin Cell      │              │        │   │
│                   VCC            VCC      VCC  │
│                    │              │        │   │
│               ┌────┴────┐    ┌───┴────┐   │   │
│               │ 0.1µF   │    │ TMP36  │   │   │
│               │   CAP   │    │ Sensor │   │   │
│               └────┬────┘    │        │   │   │
│                    │       ┌─┤1  VCC  │   │   │
│                   GND      │ │        │   │   │
│                            │ │2  Vout ├───┼───┤
│         ATtiny85           │ │        │   │   │
│        ┌────────┐          │ │3  GND  ├───┼───┤
│ RESET ─┤1  ●  8├──────────┘ └────────┘   │   │
│        │        │                         │   │
│    NC ─┤2     7├─────────────────────────┘   │
│  (PB3) │        │ (PB2/A1 ← TMP36 Vout)      │
│        │3     6├──┐                          │
│  BLUE  │        │  │ GREEN                   │
│   LED  │4     5├──┼─────────┐               │
│        └────┬───┘  │         │ RED           │
│             │      │         │               │
│            GND     │         │               │
│             │      │         │               │
│             ├──────┼─────────┼───────────────┤
│             │      │         │               │
│           ┌─┴─┐  ┌─┴─┐     ┌─┴─┐             │
│           │220│  │220│     │220│ Resistors   │
│           │Ω  │  │Ω  │     │Ω  │             │
│           └─┬─┘  └─┬─┘     └─┬─┘             │
│             │      │         │               │
│           ┌─┴──────┴─────────┴─┐             │
│           │    RGB LED          │             │
│           │  (Common Cathode)   │             │
│           │  B   GND   G    R   │             │
│           │  │    │    │    │   │             │
│           └──┼────┼────┼────┼───┘             │
│              │    │    │    │                 │
│              │    │    │    └─ PB0 (Pin 5)    │
│              │    │    └────── PB1 (Pin 6)    │
│              │    └─────────── GND (Pin 4)    │
│              └──────────────── PB4 (Pin 3)    │
│                                                │
└────────────────────────────────────────────────┘
```

### সংযোগ তালিকা:

```
Battery Connection:
┌──────────────────┬────────────────────┐
│ From             │ To                 │
├──────────────────┼────────────────────┤
│ CR2032 (+)       │ DIP Switch         │
│ DIP Switch OUT   │ ATtiny85 Pin 8 (VCC)│
│ CR2032 (─)       │ Common GND         │
└──────────────────┴────────────────────┘

TMP36 Connection:
┌──────────────────┬────────────────────┐
│ TMP36 Pin        │ ATtiny85 Pin       │
├──────────────────┼────────────────────┤
│ Pin 1 (VCC)      │ Pin 8 (VCC)        │
│ Pin 2 (Vout)     │ Pin 7 (A1)         │
│ Pin 3 (GND)      │ Pin 4 (GND)        │
└──────────────────┴────────────────────┘

RGB LED Connection:
┌──────────────────┬────────────────────┐
│ LED Pin          │ ATtiny85 Pin       │
├──────────────────┼────────────────────┤
│ Red (via 220Ω)   │ Pin 5 (PB0)        │
│ Green (via 220Ω) │ Pin 6 (PB1)        │
│ Blue (via 220Ω)  │ Pin 3 (PB4)        │
│ Cathode (long)   │ Pin 4 (GND)        │
└──────────────────┴────────────────────┘

Capacitor:
  0.1µF between VCC and GND (voltage filtering)
```

---

## 🌈 তাপমাত্রা জোন

### 5টি রঙের জোন:

এই প্রজেক্টে তাপমাত্রাকে **5টি ভাগ** করা হয়েছে, প্রতিটির জন্য আলাদা রঙ!

```
┌──────┬─────────────────┬──────────┬──────────────┬─────────────────┐
│ জোন  │ তাপমাত্রা       │ রঙ       │ RGB মান      │ মানে            │
├──────┼─────────────────┼──────────┼──────────────┼─────────────────┤
│  1   │ ≤ -20°C         │ 🔵 নীল   │ (0, 0, 255)  │ অতি ঠান্ডা     │
│  2   │ -20°C to 10°C   │ 🩵 Cyan  │ (0, 255, 255)│ ঠান্ডা         │
│  3   │ 10°C to 35°C    │ 🟢 সবুজ  │ (0, 255, 0)  │ স্বাভাবিক      │
│  4   │ 35°C to 60°C    │ 🟠 কমলা  │ (255, 165, 0)│ গরম             │
│  5   │ > 60°C          │ 🔴 লাল   │ (255, 0, 0)  │ খুব গরম         │
└──────┴─────────────────┴──────────┴──────────────┴─────────────────┘
```

### তাপমাত্রা স্কেল (Visual):

```
Temperature Line:
-40°C    -20°C     0°C     10°C    25°C    35°C    60°C   100°C
  │        │        │        │       │       │       │       │
  ├────────┼────────┼────────┼───────┼───────┼───────┼───────┤
  │  নীল   │  Cyan  │ Cyan   │ সবুজ  │ সবুজ  │ কমলা  │  লাল  │
  │ জোন ১  │ জোন ২  │ জোন ২  │ জোন ৩│ জোন ৩│ জোন ৪│ জোন ৫│
  └────────┴────────┴────────┴───────┴───────┴───────┴───────┘
  Deep     শীতকাল   ফ্রিজ    ঘরের     শরীরের  জ্বর    পানি
  Freeze            তাপমাত্রা তাপমাত্রা         ফুটছে
```

### জোন বিস্তারিত:

#### **জোন 1: 🔵 নীল (≤ -20°C)**

```
তাপমাত্রা: -20°C বা তার কম
রঙ: শুধু নীল LED জ্বলবে
মানে: অতি ঠান্ডা!

উদাহরণ:
  • Deep freezer (-30°C)
  • Antarctica (-50°C)
  • Liquid nitrogen (-196°C) ❄️
```

#### **জোন 2: 🩵 Cyan (-20°C to 10°C)**

```
তাপমাত্রা: -20°C থেকে 10°C
রঙ: সবুজ + নীল = Cyan (আকাশী)
মানে: ঠান্ডা

উদাহরণ:
  • Refrigerator (2-8°C) 🧊
  • শীতকাল (0-10°C)
  • Ice water (0°C)
```

#### **জোন 3: 🟢 সবুজ (10°C to 35°C)**

```
তাপমাত্রা: 10°C থেকে 35°C
রঙ: শুধু সবুজ LED জ্বলবে
মানে: স্বাভাবিক, আরামদায়ক

উদাহরণ:
  • ঘরের তাপমাত্রা (20-25°C) 🏠
  • শরীরের তাপমাত্রা (36.5°C)
  • গ্রীষ্মকাল (30-35°C)

এই zone-এ মানুষ comfortable!
```

#### **জোন 4: 🟠 কমলা (35°C to 60°C)**

```
তাপমাত্রা: 35°C থেকে 60°C
রঙ: লাল + সবুজ = কমলা
মানে: গরম, সতর্ক থাকুন

উদাহরণ:
  • জ্বর (38-40°C) 🤒
  • গরম আবহাওয়া (40-45°C)
  • গরম পানি (50-60°C)
  • Engine warming (50-70°C)
```

#### **জোন 5: 🔴 লাল (> 60°C)**

```
তাপমাত্রা: 60°C-এর বেশি
রঙ: শুধু লাল LED জ্বলবে
মানে: খুব গরম, বিপদ! ⚠️

উদাহরণ:
  • ফুটন্ত পানি (100°C) 💧
  • Engine overheating (>80°C)
  • হাত লাগানো যাবে না!
```

### রঙের মানে কেন এটি?

```
মানুষের মনে রঙের প্রভাব:
┌──────┬─────────────────────────┐
│ রঙ   │ মানসিক প্রভাব          │
├──────┼─────────────────────────┤
│ নীল  │ ঠান্ডা, বরফ, শীতকাল   │
│ Cyan │ শীতল, শান্ত            │
│ সবুজ │ স্বাভাবিক, নিরাপদ, OK  │
│ কমলা │ সতর্কতা, মধ্যম বিপদ    │
│ লাল  │ বিপদ, গরম, সাবধান!     │
└──────┴─────────────────────────┘

Traffic light-এর মতো:
  সবুজ = Safe ✅
  কমলা = Caution ⚠️
  লাল = Danger 🛑
```

---

## ⚙️ কাজের নীতি

### System Flow (সিস্টেম কীভাবে কাজ করে):

```
┌─────────────────────────────────────────┐
│         System Operation Flow           │
└─────────────────────────────────────────┘
              │
         Power ON (Switch)
              │
              ▼
     ┌────────────────┐
     │ ATtiny85 চালু  │
     │ • Pins setup   │
     │ • ADC চালু     │
     └────────┬───────┘
              │
              ▼
     ┌────────────────┐
     │ TMP36 পড়া     │ ◄──────────┐
     │ • A1-তে ADC    │            │
     │ • 0-1023 value │            │
     └────────┬───────┘            │
              │                    │
              ▼                    │
     ┌────────────────┐            │
     │ Voltage বের    │            │
     │ • ADC÷1023×3V  │            │
     └────────┬───────┘            │
              │                    │
              ▼                    │
     ┌────────────────┐            │
     │ তাপমাত্রা বের  │            │
     │ • Formula      │            │
     │ • (V-0.5)×100  │            │
     └────────┬───────┘            │
              │                    │
              ▼                    │
     ┌────────────────┐            │
     │ জোন চেক করা    │            │
     │ • Zone 1-5?    │            │
     │ • রঙ select    │            │
     └────────┬───────┘            │
              │                    │
              ▼                    │
     ┌────────────────┐            │
     │ RGB LED জ্বালা │            │
     │ • Red PWM      │            │
     │ • Green PWM    │            │
     │ • Blue PWM     │            │
     └────────┬───────┘            │
              │                    │
              ▼                    │
     ┌────────────────┐            │
     │ 1 সেকেন্ড wait │            │
     └────────┬───────┘            │
              │                    │
              └────────────────────┘
           (পুনরায় শুরু)
```

### ধাপে ধাপে ব্যাখ্যা:

#### **ধাপ ১: TMP36 সেন্সর পড়া**

```cpp
int raw = analogRead(tempPin);
```

```
কী হয়:
  • ATtiny85-এর ADC চালু হয়
  • A1 pin-এ voltage measure করে
  • 10-bit ADC: 0 থেকে 1023
  • 0 = 0V, 1023 = 3V

উদাহরণ:
  তাপমাত্রা 25°C হলে:
    TMP36 output = 0.75V
    ADC value = 256 (প্রায়)
```

#### **ধাপ ২: Voltage বের করা**

```cpp
float voltage = raw * (3.0 / 1023.0);
```

```
গণিত:
  voltage = (ADC / 1023) × 3V
  
উদাহরণ:
  raw = 256
  voltage = 256 × (3.0 / 1023)
  voltage = 256 × 0.00293
  voltage = 0.75V
```

#### **ধাপ ৩: তাপমাত্রা বের করা**

```cpp
float temperature = (voltage - 0.5) * 100.0;
```

```
TMP36 Formula:
  Vout = (Temp × 10mV) + 500mV
  
Reverse formula:
  Temp = (Vout - 500mV) / 10mV
  Temp = (Vout - 0.5V) × 100

উদাহরণ:
  voltage = 0.75V
  temperature = (0.75 - 0.5) × 100
  temperature = 0.25 × 100
  temperature = 25°C ✅
```

**বিভিন্ন তাপমাত্রায় voltage:**

```
┌──────────────┬─────────────┬──────────────┐
│ তাপমাত্রা    │ TMP36 Vout  │ ADC Value    │
├──────────────┼─────────────┼──────────────┤
│ -20°C        │ 0.3V        │ ~102         │
│ 0°C          │ 0.5V        │ ~170         │
│ 25°C         │ 0.75V       │ ~256         │
│ 37°C (শরীর)  │ 0.87V       │ ~296         │
│ 50°C         │ 1.0V        │ ~341         │
│ 100°C        │ 1.5V        │ ~512         │
└──────────────┴─────────────┴──────────────┘
```

#### **ধাপ ৪: জোন নির্ধারণ**

```cpp
if (temperature <= -20) {
  // জোন 1: নীল
}
else if (temperature <= 10) {
  // জোন 2: Cyan
}
else if (temperature <= 35) {
  // জোন 3: সবুজ
}
else if (temperature <= 60) {
  // জোন 4: কমলা
}
else {
  // জোন 5: লাল
}
```

```
Decision Tree:
                  তাপমাত্রা কত?
                       │
         ┌─────────────┼─────────────┐
    ≤ -20°C?      -20 to 10°C?   10 to 35°C?
      │YES            │YES           │YES
      ▼               ▼              ▼
     নীল            Cyan          সবুজ
   (Zone 1)       (Zone 2)      (Zone 3)
                                      │NO
                           ┌──────────┼──────────┐
                      35 to 60°C?          > 60°C?
                           │YES               │YES
                           ▼                  ▼
                          কমলা                লাল
                        (Zone 4)           (Zone 5)
```

#### **ধাপ ৫: RGB LED জ্বালানো**

```cpp
void setColor(int red, int green, int blue) {
  analogWrite(redPin, red);      // লাল LED
  analogWrite(greenPin, green);  // সবুজ LED
  analogWrite(bluePin, blue);    // নীল LED
}
```

```
PWM (Pulse Width Modulation):
  • 0 = LED OFF (0% duty)
  • 128 = LED HALF BRIGHT (50%)
  • 255 = LED FULL BRIGHT (100%)

উদাহরণ রঙ:
  setColor(255, 0, 0)   → শুধু লাল
  setColor(0, 255, 0)   → শুধু সবুজ
  setColor(0, 0, 255)   → শুধু নীল
  setColor(255, 165, 0) → কমলা (লাল+সবুজ)
  setColor(0, 255, 255) → Cyan (সবুজ+নীল)
```

**PWM Signal (Visual):**

```
255 (Full ON):
  ████████████████████ (100% duty)

128 (Half):
  ██████░░░░░░██████░░ (50% duty)

64 (Quarter):
  ███░░░░░░░░░███░░░░░ (25% duty)

0 (OFF):
  ░░░░░░░░░░░░░░░░░░░░ (0% duty)
```

---

## 💻 কোড ব্যাখ্যা

### সম্পূর্ণ কোড:

```cpp
/*
 * ATtiny85 Temperature Indicator
 * 5 Color Zones with RGB LED
 */

// Pin সংজ্ঞা
const int redPin = 0;      // PB0 (Pin 5) - লাল LED
const int greenPin = 1;    // PB1 (Pin 6) - সবুজ LED
const int bluePin = 4;     // PB4 (Pin 3) - নীল LED
const int tempPin = A1;    // PB2 (Pin 7) - TMP36 sensor

void setup() {
  // RGB pins OUTPUT হিসেবে সেট করা
  pinMode(redPin, OUTPUT);
  pinMode(greenPin, OUTPUT);
  pinMode(bluePin, OUTPUT);
  
  // tempPin (A1) analog input
  // pinMode লাগে না (ADC-র জন্য)
}

void loop() {
  // ধাপ ১: ADC value পড়া (0-1023)
  int raw = analogRead(tempPin);
  
  // ধাপ ২: Voltage-এ convert করা (3V reference)
  float voltage = raw * (3.0 / 1023.0);
  
  // ধাপ ৩: তাপমাত্রায় convert করা (Celsius)
  // TMP36 formula: T = (Vout - 0.5V) × 100
  float temperature = (voltage - 0.5) * 100.0;
  
  // ধাপ ৪: Color zone নির্ধারণ করা
  if (temperature <= -20) {
    // জোন 1: Deep Freeze (≤ -20°C)
    setColor(0, 0, 255);  // নীল
  }
  else if (temperature > -20 && temperature <= 10) {
    // জোন 2: Cold/Cool (-20°C to 10°C)
    setColor(0, 255, 255);  // Cyan (আকাশী)
  }
  else if (temperature > 10 && temperature <= 35) {
    // জোন 3: Comfortable (10°C to 35°C)
    setColor(0, 255, 0);  // সবুজ
  }
  else if (temperature > 35 && temperature <= 60) {
    // জোন 4: Warm/Caution (35°C to 60°C)
    setColor(255, 165, 0);  // কমলা
  }
  else {
    // জোন 5: Hot/Danger (> 60°C)
    setColor(255, 0, 0);  // লাল
  }
  
  // ধাপ ৫: 1 সেকেন্ড অপেক্ষা করা
  delay(1000);
}

// RGB LED-এর রঙ সেট করার function
void setColor(int red, int green, int blue) {
  analogWrite(redPin, red);      // লাল brightness
  analogWrite(greenPin, green);  // সবুজ brightness
  analogWrite(bluePin, blue);    // নীল brightness
}
```

---

### কোড বিস্তারিত ব্যাখ্যা:

#### **Pin সংজ্ঞা:**

```cpp
const int redPin = 0;
const int greenPin = 1;
const int bluePin = 4;
const int tempPin = A1;
```

```
ব্যাখ্যা:
  redPin = 0 → ATtiny85 PB0 (Physical Pin 5)
  greenPin = 1 → ATtiny85 PB1 (Physical Pin 6)
  bluePin = 4 → ATtiny85 PB4 (Physical Pin 3)
  tempPin = A1 → ATtiny85 PB2/A1 (Physical Pin 7)

const কেন?
  • এই values পরিবর্তন হবে না
  • Memory save হয়
```

#### **Setup Function:**

```cpp
void setup() {
  pinMode(redPin, OUTPUT);
  pinMode(greenPin, OUTPUT);
  pinMode(bluePin, OUTPUT);
}
```

```
কাজ:
  • RGB LED-এর pins OUTPUT করা
  • PWM signal পাঠানোর জন্য প্রস্তুত
  • tempPin-এর জন্য pinMode লাগে না
    (ADC pins default INPUT mode-এ থাকে)

একবারই চলে (power ON-এ)
```

#### **Loop Function (Main Code):**

```cpp
void loop() {
  // পুরো code এখানে
  // এটি infinite loop
  // বারবার চলতে থাকে
}
```

#### **ADC Reading:**

```cpp
int raw = analogRead(tempPin);
```

```
analogRead(A1):
  • A1 pin-এর voltage পড়ে
  • 0V → 0 return করে
  • 3V → 1023 return করে
  • 10-bit resolution (2^10 = 1024 values)

int type কেন?
  • ADC value সবসময় পূর্ণসংখ্যা (0-1023)
  • float লাগে না, memory বাঁচে
```

#### **Voltage Calculation:**

```cpp
float voltage = raw * (3.0 / 1023.0);
```

```
গণিত:
  voltage = (raw / 1023) × 3.0V
  
  3.0 / 1023.0 = 0.00293255 (constant)
  
float type কেন?
  • দশমিক সংখ্যা (0.5V, 0.75V, etc.)
  • নির্ভুল calculation-এর জন্য

উদাহরণ:
  raw = 512 হলে:
  voltage = 512 × 0.00293
  voltage = 1.5V
```

#### **Temperature Calculation:**

```cpp
float temperature = (voltage - 0.5) * 100.0;
```

```
TMP36 বৈশিষ্ট্য:
  • Offset: 500mV (0.5V)
  • Scale: 10mV/°C

Formula derivation:
  TMP36: Vout = (T × 0.01) + 0.5
  
  Solve for T:
  T = (Vout - 0.5) / 0.01
  T = (Vout - 0.5) × 100

উদাহরণ:
  voltage = 0.87V (শরীরের তাপমাত্রা):
  temperature = (0.87 - 0.5) × 100
  temperature = 0.37 × 100
  temperature = 37°C ✅
```

#### **If-Else Ladder (Zone Detection):**

```cpp
if (temperature <= -20) {
  setColor(0, 0, 255);  // নীল
}
else if (temperature <= 10) {
  setColor(0, 255, 255);  // Cyan
}
else if (temperature <= 35) {
  setColor(0, 255, 0);  // সবুজ
}
else if (temperature <= 60) {
  setColor(255, 165, 0);  // কমলা
}
else {
  setColor(255, 0, 0);  // লাল
}
```

```
Logic Flow:
  প্রথম condition true হলেই থেমে যায়!
  
  Example: temperature = 25°C
  
  Check 1: 25 <= -20? → NO, continue
  Check 2: 25 <= 10? → NO, continue
  Check 3: 25 <= 35? → YES! ✅
    → setColor(0, 255, 0) চলবে
    → বাকি checks skip হবে
```

**কেন && ব্যবহার করিনি?**

```cpp
// ভুল উপায়:
if (temperature > -20 && temperature <= 10) { ... }

// সঠিক উপায়:
else if (temperature <= 10) { ... }

কারণ:
  • প্রথম if এরই FALSE হলে পরের else if check হয়
  • Automatically -20-এর বেশি ensure হয়ে যায়
  • Code ছোট এবং efficient!
```

#### **setColor Function:**

```cpp
void setColor(int red, int green, int blue) {
  analogWrite(redPin, red);
  analogWrite(greenPin, green);
  analogWrite(bluePin, blue);
}
```

```
Function Parameters:
  red → 0-255 (লাল brightness)
  green → 0-255 (সবুজ brightness)
  blue → 0-255 (নীল brightness)

analogWrite():
  • PWM signal generate করে
  • Frequency: ~490 Hz
  • Duty cycle: 0-100%
  
Example:
  setColor(255, 165, 0):
    analogWrite(redPin, 255)   → 100% ON
    analogWrite(greenPin, 165) → 65% ON
    analogWrite(bluePin, 0)    → 0% OFF
    Result: কমলা রঙ! 🟠
```

#### **Delay:**

```cpp
delay(1000);
```

```
কাজ:
  • 1000 milliseconds = 1 second অপেক্ষা
  • এর পরে আবার loop() শুরু
  
কেন 1 second?
  • তাপমাত্রা তাড়াতাড়ি পরিবর্তন হয় না
  • Battery life বৃদ্ধি (কম reading)
  • LED দেখার সময় পাওয়া যায়
```

---

## 📋 পদক্ষেপসমূহ

### ধাপ ১: ATtiny85 Programming Setup

```
ATtiny85 program করতে লাগবে:
  ✅ Arduino UNO (programmer হিসেবে)
  ✅ Arduino IDE
  ✅ ATtiny Board Manager
  ✅ 10µF capacitor (RESET disable)

Steps:
1. Arduino IDE খুলুন
2. File → Preferences
3. Additional Boards Manager URLs:
   http://drazzy.com/package_drazzy.com_index.json
4. Tools → Board → Boards Manager
5. Search "ATtiny" → Install "ATTinyCore"
6. Tools → Board → ATtiny25/45/85 → ATtiny85
7. Tools → Chip → ATtiny85
8. Tools → Clock → 8 MHz (Internal)
9. Tools → Programmer → Arduino as ISP
```

### ধাপ ২: Arduino as ISP Setup

```
Arduino UNO-কে programmer বানানো:

1. File → Examples → ArduinoISP → ArduinoISP
2. Upload to Arduino UNO
3. UNO RESET-এ 10µF capacitor (disable auto-reset)

Connection:
  Arduino UNO → ATtiny85
  ──────────────────────
  Pin 13 → Pin 2 (SCK)
  Pin 12 → Pin 1 (MISO)
  Pin 11 → Pin 0 (MOSI)
  Pin 10 → Pin 1 (RESET)
  5V → Pin 8 (VCC)
  GND → Pin 4 (GND)
```

### ধাপ ৩: Code Upload to ATtiny85

```
1. attiny85-tmp36-5color.ino খুলুন
2. Tools → Board → ATtiny85 (verify)
3. Tools → Programmer → Arduino as ISP
4. Sketch → Upload Using Programmer
5. Upload complete হওয়া পর্যন্ত অপেক্ষা করুন ✅
```

### ধাপ ৪: Circuit Assembly

**Breadboard-এ লাগানো:**

```
1. ATtiny85 DIP socket-এ insert করুন (যদি থাকে)
2. TMP36 breadboard-এ রাখুন
3. RGB LED রাখুন (লম্বা pin = cathode)
4. 220Ω resistors রাখুন (প্রতি color-এ 1টি)
5. CR2032 battery holder সংযুক্ত করুন
6. DIP switch যোগ করুন
7. 0.1µF capacitor (VCC-GND মাঝে)
```

**Connection Checklist:**

```
✅ Battery (+) → Switch → ATtiny85 Pin 8 (VCC)
✅ Battery (─) → GND rail
✅ ATtiny85 Pin 4 → GND
✅ TMP36 Pin 1 → VCC
✅ TMP36 Pin 2 → ATtiny85 Pin 7 (A1)
✅ TMP36 Pin 3 → GND
✅ Red LED → 220Ω → ATtiny85 Pin 5
✅ Green LED → 220Ω → ATtiny85 Pin 6
✅ Blue LED → 220Ω → ATtiny85 Pin 3
✅ RGB Cathode → GND
✅ 0.1µF capacitor: VCC-GND
```

### ধাপ ৫: Testing

```
1. Switch ON করুন
2. LED জ্বলছে কিনা দেখুন (room temp = সবুজ)
3. TMP36 গরম করুন (হাত দিয়ে):
   → সবুজ → কমলা → লাল
4. ঠান্ডা করুন (ice cube):
   → সবুজ → Cyan → নীল
```

### ধাপ ৬: Calibration (যদি লাগে)

```
Temperature ভুল দেখালে:

1. Reference thermometer দিয়ে actual temp মাপুন
2. Code-এ offset যোগ করুন:

// Code-এ এই line যোগ করুন:
float temperature = (voltage - 0.5) * 100.0;
temperature += CALIBRATION_OFFSET;  // উপরে define করুন

// উদাহরণ:
#define CALIBRATION_OFFSET -2.5  // 2.5°C কমানোর জন্য
```

---

## 🔧 সমস্যা সমাধান

### সাধারণ সমস্যা ও সমাধান:

```
┌────────────────────────┬────────────────────┬─────────────────────┐
│ সমস্যা                 │ কারণ               │ সমাধান             │
├────────────────────────┼────────────────────┼─────────────────────┤
│ LED জ্বলছে না          │ Battery dead       │ নতুন CR2032 লাগান  │
│                        │ Switch OFF         │ Switch ON করুন     │
│                        │ Loose wire         │ Connection check    │
├────────────────────────┼────────────────────┼─────────────────────┤
│ ভুল রঙ দেখাচ্ছে         │ Pin mapping ভুল    │ Pin verify করুন    │
│                        │ TMP36 উল্টা        │ Pinout check করুন  │
├────────────────────────┼────────────────────┼─────────────────────┤
│ সবসময় লাল জ্বলছে      │ TMP36 disconnect   │ Vout wire চেক করুন │
│                        │ Sensor গরম হয়েছে   │ ঠান্ডা করুন        │
├────────────────────────┼────────────────────┼─────────────────────┤
│ LED flickering         │ Loose connection   │ Wire secure করুন    │
│                        │ Low battery (<2.7V)│ Battery replace     │
├────────────────────────┼────────────────────┼─────────────────────┤
│ রঙ পরিবর্তন হচ্ছে না    │ Code upload হয়নি   │ Re-upload code      │
│                        │ Formula ভুল        │ Voltage ref check   │
├────────────────────────┼────────────────────┼─────────────────────┤
│ Common anode RGB LED   │ LED type ভুল       │ Common cathode কিনুন│
└────────────────────────┴────────────────────┴─────────────────────┘
```

### Diagnostic Code:

```cpp
// LED দিয়ে temperature বের করা (debugging)
void blinkTemperature() {
  int raw = analogRead(tempPin);
  float voltage = raw * (3.0 / 1023.0);
  float temperature = (voltage - 0.5) * 100.0;
  
  // 1 blink = 10°C
  int blinks = temperature / 10;
  
  for (int i = 0; i < blinks; i++) {
    setColor(255, 255, 255);  // সাদা flash
    delay(200);
    setColor(0, 0, 0);        // OFF
    delay(200);
  }
  delay(2000);
}

// Example: 25°C → 2-3 blinks
```

### Multimeter দিয়ে Testing:

```
TMP36 Pin Test (Voltage mode):
┌────────────┬───────────────┬──────────────┐
│ Pin        │ Expected      │ মানে         │
├────────────┼───────────────┼──────────────┤
│ Pin 1 (VCC)│ ~3.0V         │ Power আছে    │
│ Pin 2 (Vout)│ 0.5V - 2.5V  │ Sensor OK    │
│ Pin 3 (GND)│ 0V            │ Ground OK    │
└────────────┴───────────────┴──────────────┘

যদি Pin 2 সবসময় 3V বা 0V দেখায়:
  → TMP36 উল্টা লাগানো বা নষ্ট!
```

---

## 🎓 শিক্ষণীয় বিষয়

### এই প্রজেক্ট থেকে কী শিখলাম:

```
✅ ATtiny85 microcontroller programming
✅ Low-power circuit design (battery-operated)
✅ Analog sensor (TMP36) interfacing
✅ ADC (Analog-to-Digital Conversion)
✅ Voltage-to-temperature conversion
✅ PWM দিয়ে RGB LED control
✅ Color mixing (Red + Green = Orange)
✅ 3V system constraints
✅ Conditional logic (if-else zones)
✅ Power management
```

### Advanced Concepts:

```
1. Voltage Reference:
   • ATtiny85-এ VCC = ADC reference
   • 3V system → max 3V input
   • TMP36 max = VCC - 0.2V

2. PWM (Pulse Width Modulation):
   • 490 Hz frequency
   • Duty cycle: 0-100%
   • analogWrite(pin, 0-255)

3. Battery Life:
   • Current: ~20mA (typical)
   • Capacity: 220mAh
   • Life: 11 hours (continuous)

4. Thermal Response:
   • TMP36 response time: ~30s
   • Thermal mass effects
   • Heat transfer principles
```

### পরবর্তী প্রজেক্ট Ideas:

```
1. 🌡️ Body Temperature Monitor (wearable)
2. 🧊 Fridge Temperature Alarm
3. 💻 CPU Cooling Monitor
4. 🌱 Greenhouse Climate Control
5. 🚗 Car Engine Temperature
6. 🍼 Baby Room Monitor
7. 🏊 Swimming Pool Thermometer
```

---

## 💡 Tips & Tricks

### Power Saving:

```cpp
// LED brightness কমিয়ে battery life বাড়ানো
void setColor(int red, int green, int blue) {
  // 50% brightness = 2x battery life!
  analogWrite(redPin, red / 2);
  analogWrite(greenPin, green / 2);
  analogWrite(bluePin, blue / 2);
}
```

### Custom Zones:

```cpp
// নিজের জোন বানান!
// Example: ফ্রিজের জন্য:
if (temperature < 0) {
  setColor(0, 0, 255);  // Freezing
}
else if (temperature <= 8) {
  setColor(0, 255, 0);  // OK
}
else {
  setColor(255, 0, 0);  // Too warm!
}
```

### Fahrenheit Conversion:

```cpp
// °C থেকে °F convert করা
float fahrenheit = (temperature * 9.0 / 5.0) + 32.0;

// Fahrenheit zones:
// ≤ -4°F: Blue
// -4°F to 50°F: Cyan
// 50°F to 95°F: Green
// 95°F to 140°F: Orange
// > 140°F: Red
```

---

## ❓ প্রশ্নোত্তর

**প্রশ্ন ১: ATtiny85 কেন Arduino UNO-র চেয়ে ভালো এই প্রজেক্টে?**
- উত্তর: ছোট size, কম power consumption, portable, সস্তা!

**প্রশ্ন ২: Battery কতক্ষণ চলবে?**
- উত্তর: CR2032 (220mAh) দিয়ে প্রায় 10-12 ঘণ্টা।

**প্রশ্ন ৩: Common anode RGB LED ব্যবহার করা যাবে?**
- উত্তর: না! Common cathode লাগবে। Anode-এর জন্য code পরিবর্তন করতে হবে।

**প্রশ্ন ৪: 3V-এ সর্বোচ্চ কত তাপমাত্রা মাপা যাবে?**
- উত্তর: TMP36-এর theoretical limit ~250°C, কিন্তু sensor-এর max = 125°C।

**প্রশ্ন ৫: Calibration কেন লাগবে?**
- উত্তর: TMP36-এর ±2°C tolerance আছে, নির্ভুল measurement-এর জন্য calibration করা ভালো।

---

## 👨‍🎓 লেখক

**মো. আখিনুর ইসলাম**  
📚 Energy Science and Engineering (ESE)  
🏫 Khulna University of Engineering & Technology (KUET)

---

## 📄 License

MIT License - Free to use and modify!

---

### ✨ সফলতার চাবিকাঠি:

```
1. TMP36 pinout সঠিকভাবে চেক করুন (VCC, Vout, GND)
2. Common Cathode RGB LED ব্যবহার করুন
3. Battery voltage >2.7V রাখুন
4. Connection secure করুন (loose wire সমস্যা করে)
5. Ice water (0°C) দিয়ে calibrate করুন
6. Program upload-এর আগে সব connection check করুন
```

**শুভকামনা! Portable temperature indicator বানান! 🌡️🎉**
