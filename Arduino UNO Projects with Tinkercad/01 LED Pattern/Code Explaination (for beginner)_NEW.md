# 🔰 বিগিনারদের জন্য সম্পূর্ণ গাইড | Complete Beginner's Guide

## 📚 সূচিপত্র | Table of Contents
- [শেখার পয়েন্ট](#-শেখার-পয়েন্ট--what-youll-learn)
- [প্রয়োজনীয় যন্ত্রাংশ](#-প্রয়োজনীয়-যন্ত্রাংশ--components-needed)
- [সার্কিট গাইড](#%EF%B8%8F-সার্কিট-গাইড--circuit-guide)
- [কোড ব্যাখ্যা](#-কোড-ব্যাখ্যা--code-explanation)
- [কিভাবে কাজ করে](#-কিভাবে-কাজ-করে--how-it-works)
- [সমস্যা সমাধান](#-সমস্যা-সমাধান--troubleshooting)
- [নিজে করে দেখো](#-নিজে-করে-দেখো--try-it-yourself)

---

## 🔰 শেখার পয়েন্ট | What You'll Learn

এই প্রজেক্ট থেকে তুমি যা যা শিখবে:

| বিষয় | ব্যাখ্যা |
|-------|---------|
| **analogRead()** | কিভাবে potentiometer-এর মান পড়তে হয় (0-1023 রেঞ্জে) |
| **digitalWrite()** | কিভাবে LED জ্বালানো এবং নেভানো যায় |
| **if / else if** | কিভাবে বিভিন্ন voltage level-এ LED কন্ট্রোল করতে হয় |
| **Arrays (অ্যারে)** | একসাথে অনেকগুলো পিন নম্বর সংরক্ষণ করা |
| **For Loop** | একই কাজ বারবার করানোর সহজ উপায় |
| **Serial Monitor** | কোড ডিবাগ করার জন্য মান দেখা |
| **Voltage Threshold** | নির্দিষ্ট ভোল্টেজে LED চালু/বন্ধ করা |
| **পিনের ব্যবহার** | A0 analog input, D2–D6 digital output কেন ও কিভাবে |

---

## 🧰 প্রয়োজনীয় যন্ত্রাংশ | Components Needed

### যা যা লাগবে:

| যন্ত্রাংশের নাম | সংখ্যা | বিবরণ |
|----------------|--------|-------|
| Arduino UNO | ১টি | মূল কন্ট্রোলার বোর্ড |
| Breadboard | ১টি | সার্কিট বানানোর জন্য |
| LED | ৫টি | যেকোন রঙের (লাল/সবুজ/হলুদ) |
| 220Ω Resistor | ৫টি | LED সুরক্ষার জন্য (রঙ: লাল-লাল-বাদামি) |
| Potentiometer | ১টি | 10kΩ (ভোল্টেজ নিয়ন্ত্রণের জন্য) |
| Jumper Wire | ১৫-২০টি | সংযোগের জন্য (পুরুষ-থেকে-পুরুষ) |

### 💰 আনুমানিক খরচ: ৭০০-১০০০ টাকা

---

## 🛠️ সার্কিট গাইড | Circuit Guide

### 🔌 Step 1: Potentiometer সংযোগ

```
Potentiometer এর তিনটি পিন আছে:
┌─────────────────┐
│    [Terminal 1] │ ──────► 5V (Arduino)
│    [Wiper/মাঝ]  │ ──────► A0 (Analog Pin)
│    [Terminal 2] │ ──────► GND (Ground)
└─────────────────┘
```

**বিস্তারিত:**
- **এক পাশের পিন (Terminal 1)** → Arduino-এর 5V পিনে সংযুক্ত করো
- **মাঝের পিন (Wiper/Variable)** → Arduino-এর A0 পিনে সংযুক্ত করো
- **অন্য পাশের পিন (Terminal 2)** → Arduino-এর GND পিনে সংযুক্ত করো

> 💡 **টিপস:** Potentiometer ঘুরালে মাঝের পিনের ভোল্টেজ 0V থেকে 5V পর্যন্ত পরিবর্তন হয়।

---

### 💡 Step 2: LED সংযোগ

প্রতিটি LED-এর দুটি পা আছে:
- **লম্বা পা (Anode/+)** → Resistor এর মাধ্যমে Arduino pin-এ যাবে
- **ছোট পা (Cathode/-)** → GND লাইনে যাবে

```
LED সংযোগ টেবিল:
┌──────────┬──────────────┬─────────────────┐
│ LED No.  │ Arduino Pin  │ Connection      │
├──────────┼──────────────┼─────────────────┤
│ LED 1    │ D2           │ ──[220Ω]──[D2]  │
│ LED 2    │ D3           │ ──[220Ω]──[D3]  │
│ LED 3    │ D4           │ ──[220Ω]──[D4]  │
│ LED 4    │ D5           │ ──[220Ω]──[D5]  │
│ LED 5    │ D6           │ ──[220Ω]──[D6]  │
└──────────┴──────────────┴─────────────────┘

সব LED-এর ছোট পা → Breadboard-এর GND লাইনে
```

**বিস্তারিত পদ্ধতি:**
1. Breadboard-এ ৫টি LED বসাও
2. প্রতিটি LED-এর লম্বা পায়ের সাথে একটি করে 220Ω resistor লাগাও
3. Resistor-এর অন্য প্রান্ত Arduino-এর D2, D3, D4, D5, D6 পিনে সংযুক্ত করো
4. সব LED-এর ছোট পা Breadboard-এর GND (-) লাইনে সংযুক্ত করো

---

### 🔋 Step 3: Power Connection

```
Arduino → Breadboard Power Rails:
  Arduino 5V  ──────► Breadboard (+) Rail
  Arduino GND ──────► Breadboard (−) Rail
```

---

### 📊 সম্পূর্ণ সার্কিট ডায়াগ্রাম:

```
        Arduino UNO
      ┌─────────────┐
   5V │●            │
  GND │●            │
   A0 │●            │ ← Potentiometer মাঝের পিন
      │             │
   D2 │●────[220Ω]──┤──[LED1]──┐
   D3 │●────[220Ω]──┤──[LED2]──┤
   D4 │●────[220Ω]──┤──[LED3]──┼───► GND Rail
   D5 │●────[220Ω]──┤──[LED4]──┤
   D6 │●────[220Ω]──┤──[LED5]──┘
      └─────────────┘
```

---

## 🧑‍💻 কোড ব্যাখ্যা | Code Explanation

### পুরো কোডটি এক নজরে:

```cpp
/*
 * প্রজেক্ট: LED Pattern with Potentiometer
 * লেখক: Md. Akhinoor Islam
 * বিবরণ: Potentiometer ঘুরিয়ে ৫টি LED নিয়ন্ত্রণ করা
 */

// 1️⃣ ভেরিয়েবল ডিক্লেয়ারেশন
const int potPin = A0;              // Potentiometer-এর মাঝের পিন A0 তে
const int leds[] = {2, 3, 4, 5, 6}; // ৫টা LED-এর পিন নম্বর

// 2️⃣ একবার চলবে (Arduino চালু হলে)
void setup() {
  // সব LED পিনকে OUTPUT বানানো
  for (int i = 0; i < 5; i++) {
    pinMode(leds[i], OUTPUT);
  }
  Serial.begin(9600); // Serial Monitor চালু
}

// 3️⃣ বারবার চলতে থাকবে
void loop() {
  // Potentiometer-এর মান পড়া (0 থেকে 1023)
  int potValue = analogRead(potPin);
  Serial.println(potValue); // মান দেখানো

  // ভোল্টেজ অনুযায়ী LED জ্বালানো
  if (potValue < 200) {
    // Zone 1: শুধু 1টি LED
    digitalWrite(leds[0], HIGH);
    for (int i = 1; i < 5; i++) digitalWrite(leds[i], LOW);
  } 
  else if (potValue < 400) {
    // Zone 2: 2টি LED
    for (int i = 0; i < 2; i++) digitalWrite(leds[i], HIGH);
    for (int i = 2; i < 5; i++) digitalWrite(leds[i], LOW);
  } 
  else if (potValue < 600) {
    // Zone 3: 3টি LED
    for (int i = 0; i < 3; i++) digitalWrite(leds[i], HIGH);
    for (int i = 3; i < 5; i++) digitalWrite(leds[i], LOW);
  } 
  else if (potValue < 800) {
    // Zone 4: 4টি LED
    for (int i = 0; i < 4; i++) digitalWrite(leds[i], HIGH);
    digitalWrite(leds[4], LOW);
  } 
  else {
    // Zone 5: সব LED (5টি)
    for (int i = 0; i < 5; i++) digitalWrite(leds[i], HIGH);
  }

  delay(300); // ৩০০ মিলিসেকেন্ড অপেক্ষা
}
```

---

### 🔍 লাইন বাই লাইন ব্যাখ্যা:

#### 1️⃣ **ভেরিয়েবল ডিক্লেয়ারেশন (Variable Declaration)**

```cpp
const int potPin = A0;
```
- **কি বোঝায়:** Potentiometer-এর মাঝের পিন A0 তে লাগানো আছে
- **const int:** এই মান পরে পরিবর্তন করা যাবে না
- **A0:** Arduino-এর analog input pin

```cpp
const int leds[] = {2, 3, 4, 5, 6};
```
- **কি বোঝায়:** ৫টি LED-এর পিন নম্বর একটি Array-তে রাখা হয়েছে
- **Array:** একসাথে অনেক মান রাখার উপায়
- **leds[0]** = 2, **leds[1]** = 3, ... এভাবে

---

#### 2️⃣ **সেটআপ ফাংশন (setup())**

```cpp
void setup() {
  for (int i = 0; i < 5; i++) {
    pinMode(leds[i], OUTPUT);
  }
  Serial.begin(9600);
}
```

**বিস্তারিত ব্যাখ্যা:**

| লাইন | কাজ |
|------|-----|
| `void setup()` | Arduino চালু হলে একবার মাত্র চলবে |
| `for (int i = 0; i < 5; i++)` | 0 থেকে 4 পর্যন্ত লুপ চলবে (মোট 5 বার) |
| `pinMode(leds[i], OUTPUT)` | প্রতিটি LED পিনকে OUTPUT বানানো হচ্ছে |
| `Serial.begin(9600)` | Serial Monitor চালু করা হচ্ছে debug-এর জন্য |

> 💡 **কেন OUTPUT?** কারণ আমরা LED-কে সিগন্যাল পাঠাবো (জ্বালাবো/নেভাবো)

---

#### 3️⃣ **লুপ ফাংশন (loop())**

```cpp
int potValue = analogRead(potPin);
```
- **analogRead():** A0 পিন থেকে voltage পড়ে
- **রেঞ্জ:** 0 (0V) থেকে 1023 (5V)
- **উদাহরণ:** যদি 2.5V হয়, তাহলে মান হবে প্রায় 512

```cpp
Serial.println(potValue);
```
- Serial Monitor-এ মান দেখায়
- Debug করতে সাহায্য করে

---

### 🎚️ Voltage Zone Logic:

```cpp
if (potValue < 200) {
  digitalWrite(leds[0], HIGH);  // LED 1 জ্বলবে
  for (int i = 1; i < 5; i++) digitalWrite(leds[i], LOW); // বাকি নিভবে
}
```

**Voltage Zone টেবিল:**

| Zone | Potentiometer মান | ভোল্টেজ | কয়টি LED জ্বলবে |
|------|-------------------|---------|-----------------|
| 1 | 0 - 199 | 0V - 0.97V | ১টি (LED 1) |
| 2 | 200 - 399 | 0.98V - 1.95V | ২টি (LED 1-2) |
| 3 | 400 - 599 | 1.96V - 2.93V | ৩টি (LED 1-3) |
| 4 | 600 - 799 | 2.94V - 3.91V | ৪টি (LED 1-4) |
| 5 | 800 - 1023 | 3.92V - 5V | ৫টি (সব LED) |

```cpp
delay(300);
```
- ৩০০ মিলিসেকেন্ড (০.৩ সেকেন্ড) অপেক্ষা করে
- এটা না থাকলে LED খুব দ্রুত জ্বলবে-নিভবে

---

## ⚙️ কিভাবে কাজ করে | How It Works

### 📖 সহজ ভাষায়:

1. **Potentiometer ঘোরানো:**
   - ঘড়ির কাঁটার দিকে ঘুরালে → ভোল্টেজ বাড়ে (0V থেকে 5V)
   - বিপরীতে ঘুরালে → ভোল্টেজ কমে

2. **Arduino মান পড়ে:**
   - A0 pin দিয়ে ভোল্টেজ পড়ে
   - 0-5V কে 0-1023 সংখ্যায় রূপান্তর করে

3. **LED নিয়ন্ত্রণ:**
   - কম ভোল্টেজ (0-199) → ১টি LED জ্বলে
   - মাঝারি ভোল্টেজ (200-399) → ২টি LED জ্বলে
   - বেশি ভোল্টেজ (800+) → সব LED জ্বলে

---

## 🔧 সমস্যা সমাধান | Troubleshooting

### সাধারণ সমস্যা এবং সমাধান:

| সমস্যা | কারণ | সমাধান |
|--------|------|--------|
| LED জ্বলছে না | উল্টা সংযোগ | LED-এর লম্বা পা resistor-এর দিকে আছে কিনা চেক করো |
| সব LED সব সময় জ্বলে | Potentiometer সংযুক্ত নয় | A0, 5V, GND সংযোগ ঠিক আছে কিনা দেখো |
| Serial Monitor-এ কিছু দেখা যাচ্ছে না | Baud rate ভুল | 9600 সিলেক্ট করো |
| LED কখনো জ্বলে, কখনো নিভে | তার ঢিলা | সব তার শক্ত করে চেপে দাও |
| Arduino detect হচ্ছে না | Driver নাই | CH340 driver install করো |

---

## 🎯 সংক্ষেপে | In Short

### ৩টি মূল বিষয়:
1. **Potentiometer ঘুরালে** → analog value বদলায় (0-1023)
2. **Arduino সেই মান পড়ে** → ভোল্টেজ অনুযায়ী zone নির্ধারণ করে
3. **সেই zone অনুযায়ী** → LED জ্বলে/নিভে

### 📊 ডায়াগ্রাম:

```
Potentiometer ঘোরানো
        ↓
   Voltage পরিবর্তন (0-5V)
        ↓
  Arduino A0 পিনে পড়ে
        ↓
  Digital মান (0-1023)
        ↓
   if-else logic চেক
        ↓
  LED on/off হয়
```

---

## ✅ নিজে করে দেখো | Try It Yourself!

### 📝 Step-by-Step চেকলিস্ট:

- [ ] সব যন্ত্রাংশ সংগ্রহ করো
- [ ] Breadboard-এ সার্কিট বানাও
- [ ] সংযোগ ঠিক আছে কিনা দুবার চেক করো
- [ ] Arduino IDE খোলো
- [ ] কোড কপি করে paste করো
- [ ] Board এবং Port সিলেক্ট করো
- [ ] Upload বাটনে ক্লিক করো
- [ ] Serial Monitor খোলো (Ctrl+Shift+M)
- [ ] Baud rate 9600 সেট করো
- [ ] Potentiometer আস্তে আস্তে ঘুরাও
- [ ] LED pattern দেখো এবং Serial Monitor-এ মান দেখো

---

### 🚀 পরবর্তী লেভেল (Advanced):

একবার এই প্রজেক্ট সফল হলে চেষ্টা করো:

1. **আরো LED যুক্ত করো** (D7, D8, D9...)
2. **বিভিন্ন রঙের LED** ব্যবহার করো
3. **Blinking pattern** তৈরি করো
4. **LCD Display** যুক্ত করে voltage দেখাও
5. **PWM** দিয়ে LED-এর brightness নিয়ন্ত্রণ করো

---

### 📚 আরও শিখতে:

- **Analog vs Digital**: A0-A5 analog, D0-D13 digital
- **PWM Pins**: D3, D5, D6, D9, D10, D11 (~ চিহ্ন আছে)
- **Serial Communication**: 9600, 115200 baud rate
- **Resistor Color Code**: বাদামি-কালো-লাল = 1kΩ

---

## 🎓 শেষ কথা:

এই প্রজেক্ট করে তুমি শিখেছো:
- ✅ Analog signal পড়তে
- ✅ Digital output নিয়ন্ত্রণ করতে
- ✅ Conditional logic লিখতে
- ✅ Arrays এবং Loops ব্যবহার করতে
- ✅ Serial Monitor দিয়ে debug করতে

**শুভ কামনা রইলো! Keep Learning! 🎉**

---

## 👨‍🎓 প্রজেক্ট তৈরি করেছেন:

**Md Akhinoor Islam**  
Energy Science and Engineering (ESE)  
Khulna University of Engineering & Technology (KUET)

🌐 [GitHub](https://github.com/Akhinoor14) | 🔗 [Tinkercad Simulation](https://www.tinkercad.com/things/c3b3O8IGfQV-01-led-pattern)
