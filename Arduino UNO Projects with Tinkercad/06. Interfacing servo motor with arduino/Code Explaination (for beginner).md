# 🤖 সার্ভো মোটর নিয়ন্ত্রণ - Arduino দিয়ে (বিস্তারিত ব্যাখ্যা)

![Arduino Bangla Tutorial](https://img.shields.io/badge/ভাষা-বাংলা-green?style=for-the-badge)
![Difficulty](https://img.shields.io/badge/লেভেল-শিক্ষানবিস-brightgreen?style=for-the-badge)
![Servo Control](https://img.shields.io/badge/সার্ভো-মোটর-orange?style=for-the-badge)

---

## 📚 সূচিপত্র
- [প্রজেক্ট পরিচিতি](#-প্রজেক্ট-পরিচিতি)
- [সার্ভো মোটর কী?](#-সার্ভো-মোটর-কী)
- [প্রয়োজনীয় যন্ত্রপাতি](#-প্রয়োজনীয়-যন্ত্রপাতি)
- [সার্কিট সংযোগ](#-সার্কিট-সংযোগ)
- [কোড ব্যাখ্যা](#-কোড-ব্যাখ্যা)
- [কীভাবে কাজ করে](#-কীভাবে-কাজ-করে)
- [সমস্যা ও সমাধান](#-সমস্যা-ও-সমাধান)
- [শিক্ষণীয় বিষয়](#-শিক্ষণীয়-বিষয়)

---

## 🎯 প্রজেক্ট পরিচিতি

এই প্রজেক্টে আমরা Arduino দিয়ে একটি **সার্ভো মোটর** নিয়ন্ত্রণ করব। সার্ভো মোটরটি 0° থেকে 180° পর্যন্ত ঘুরবে এবং আবার ফিরে আসবে।

### এই প্রজেক্ট থেকে শিখবেন:
- ✅ সার্ভো মোটর কীভাবে কাজ করে
- ✅ PWM সিগন্যাল দিয়ে কীভাবে কোণ নিয়ন্ত্রণ করতে হয়
- ✅ Arduino Servo Library ব্যবহার
- ✅ রোবটিক্স প্রজেক্টের ভিত্তি

---

## 🔬 সার্ভো মোটর কী?

### সাধারণ ভাষায়:

**সার্ভো মোটর** হলো এমন একটি মোটর যা নির্দিষ্ট কোণে ঘুরতে পারে। সাধারণ DC মোটর যেখানে শুধু ঘোরতে থাকে, সার্ভো মোটর নির্দিষ্ট angle-এ থেমে থাকতে পারে।

### সার্ভো মোটরের প্রকারভেদ:

| ধরণ | ঘূর্ণন সীমা | ব্যবহার |
|-----|------------|---------|
| **স্ট্যান্ডার্ড সার্ভো** | 0° থেকে 180° | রোবট, RC গাড়ি (SG90, MG90S) |
| **কন্টিনিউয়াস রোটেশন** | 360° লাগাতার ঘোরে | চাকা চালানোর জন্য |
| **ইন্ডাস্ট্রিয়াল সার্ভো** | একাধিক ঘূর্ণন | উচ্চ নির্ভুলতা প্রয়োজন |

### সার্ভো মোটরের ভেতরে কী আছে?

```
সার্ভো মোটরের অভ্যন্তরীণ গঠন:
┌─────────────────────────────────┐
│  DC মোটর (ছোট)                 │
│    ↓                            │
│  গিয়ার সিস্টেম (শক্তি বাড়ায়)  │
│    ↓                            │
│  আউটপুট শ্যাফট (যেটা ঘোরে)     │
│    ↓                            │
│  পটেনশিওমিটার (position check)  │
│    ↓                            │
│  কন্ট্রোল সার্কিট (brain)       │
└─────────────────────────────────┘

নিয়ন্ত্রণ সিগন্যাল: PWM
  • Pulse width দিয়ে angle ঠিক হয়
  • 1ms pulse = 0°
  • 1.5ms pulse = 90°
  • 2ms pulse = 180°
```

### SG90 সার্ভোর বিস্তারিত:

| বৈশিষ্ট্য | মান |
|----------|-----|
| ভোল্টেজ | 4.8V - 6V |
| কারেন্ট | ~100-250mA (চলন্ত অবস্থায়) |
| সর্বোচ্চ কারেন্ট | ~650mA (আটকে গেলে) |
| টর্ক | 1.8 kg·cm (4.8V-এ) |
| গতি | 0.1s/60° (4.8V-এ) |
| ঘূর্ণন সীমা | 0° - 180° |
| সিগন্যাল ফ্রিকোয়েন্সি | 50 Hz (PWM) |

### তারের রঙ চেনার উপায়:

```
সার্ভো মোটরের তার:
┌────────────────────────────────┐
│  তার     │  রঙ            │  কানেকশন    │
│──────────┼────────────────┼──────────────│
│  পাওয়ার  │  লাল           │  5V          │
│  গ্রাউন্ড │  বাদামী/কালো  │  GND         │
│  সিগন্যাল │  কমলা/হলুদ     │  PWM Pin     │
└────────────────────────────────┘

মনে রাখার কৌশল:
  লাল = বিদ্যুৎ (সবসময় red মানে power)
  কালো/বাদামী = মাটি (গাঢ় রঙ = ground)
  উজ্জ্বল রঙ = সিগন্যাল
```

---

## 🧰 প্রয়োজনীয় যন্ত্রপাতি

| যন্ত্রের নাম | সংখ্যা | বিবরণ |
|-------------|--------|-------|
| Arduino UNO | ১টি | ATmega328P মাইক্রোকন্ট্রোলার |
| সার্ভো মোটর | ১টি | SG90 বা অনুরূপ (180° ঘূর্ণন) |
| ব্রেডবোর্ড | ১টি | (ঐচ্ছিক) সংযোগের জন্য |
| জাম্পার তার | ৩টি | Male-to-Male |

### 💰 আনুমানিক খরচ: ৫০০-৭০০ টাকা

---

## 🔌 সার্কিট সংযোগ

### তার সংযোগ টেবিল:

| সার্ভোর তার | Arduino পিন | ব্যাখ্যা |
|-------------|-------------|---------|
| লাল তার | 5V | বিদ্যুৎ সরবরাহ (4.8V-6V) |
| বাদামী/কালো তার | GND | গ্রাউন্ড (মাটি) |
| কমলা/হলুদ/সাদা তার | D9 | PWM সিগন্যাল (নিয়ন্ত্রণ) |

### সার্কিট ডায়াগ্রাম:

```
Arduino UNO                    সার্ভো মোটর (SG90)
┌─────────────┐               ┌────────────────┐
│             │               │                │
│   5V  ●─────┼───────────────┤ লাল তার         │
│             │               │ (Power)        │
│             │               │                │
│  GND  ●─────┼───────────────┤ বাদামী তার      │
│             │               │ (Ground)       │
│             │               │                │
│   D9  ●─────┼───────────────┤ কমলা তার        │
│  (PWM)      │               │ (Signal)       │
│             │               │                │
└─────────────┘               └────────────────┘

D9 pin থেকে PWM সিগন্যাল যায়
সার্ভো নির্দেশ অনুযায়ী ঘোরে
```

### ⚠️ গুরুত্বপূর্ণ সতর্কতা:

```
বিদ্যুৎ সংক্রান্ত সাবধানতা:
┌─────────────────────────────────┐
│ ১টি সার্ভো:                    │
│   → Arduino 5V যথেষ্ট          │
│                                 │
│ ২-৩টি সার্ভো:                  │
│   → External 5V 1A প্রয়োজন    │
│                                 │
│ ৪+ সার্ভো:                     │
│   → External 5V 2A+ প্রয়োজন   │
└─────────────────────────────────┘

❌ ভুল পোলারিটি Arduino নষ্ট করতে পারে!
✅ দুইবার চেক করুন: Red=5V, Brown=GND
```

---

## 💻 কোড ব্যাখ্যা

### সম্পূর্ণ কোড:

```cpp
/*
 * প্রজেক্ট: সার্ভো মোটর নিয়ন্ত্রণ
 * লেখক: Md. Akhinoor Islam
 * বিবরণ: সার্ভো 0° থেকে 180° পর্যন্ত smooth ভাবে ঘোরে
 */

#include <Servo.h>  // Servo লাইব্রেরি যুক্ত করা

Servo myServo;      // সার্ভো object তৈরি

void setup() {
  myServo.attach(9);  // সার্ভো pin 9-এ সংযুক্ত করা
}

void loop() {
  // 0° থেকে 180° পর্যন্ত যাওয়া
  for (int angle = 0; angle <= 180; angle += 1) {
    myServo.write(angle);  // angle-এ চলে যাও
    delay(15);             // সার্ভো পৌঁছানোর জন্য অপেক্ষা
  }

  // 180° থেকে 0°-এ ফিরে আসা
  for (int angle = 180; angle >= 0; angle -= 1) {
    myServo.write(angle);  // angle-এ চলে যাও
    delay(15);             // সার্ভো পৌঁছানোর জন্য অপেক্ষা
  }
}
```

---

### 📖 লাইন-বাই-লাইন ব্যাখ্যা:

#### ১. লাইব্রেরি যুক্ত করা

```cpp
#include <Servo.h>
```

**কী করে?**
- Arduino-র Servo লাইব্রেরি ব্যবহার করার জন্য
- এই লাইব্রেরি PWM সিগন্যাল তৈরি করে দেয় automatically

**লাইব্রেরি কেন প্রয়োজন?**
- সার্ভো মোটর চালাতে জটিল PWM timing দরকার
- Manually PWM করা কঠিন এবং সময়সাপেক্ষ
- লাইব্রেরি সব জটিলতা লুকিয়ে রাখে

---

#### ২. সার্ভো object তৈরি

```cpp
Servo myServo;
```

**কী করে?**
- `myServo` নামে একটি servo object তৈরি করে
- একাধিক সার্ভো থাকলে একাধিক object তৈরি করতে হবে

**উদাহরণ:**
```cpp
Servo servo1;  // প্রথম সার্ভো
Servo servo2;  // দ্বিতীয় সার্ভো
Servo servo3;  // তৃতীয় সার্ভো
```

---

#### ৩. Setup ফাংশন

```cpp
void setup() {
  myServo.attach(9);
}
```

**কী করে?**
- সার্ভো মোটরকে pin 9-এর সাথে সংযুক্ত করে
- PWM সিগন্যাল তৈরির প্রস্তুতি নেয়

**`attach()` ফাংশন:**

| প্যারামিটার | বিবরণ | উদাহরণ |
|------------|-------|--------|
| `pin` | যে pin-এ সার্ভো লাগানো | 9 |
| (ঐচ্ছিক) `min` | Minimum pulse width | 544 μs (default) |
| (ঐচ্ছিক) `max` | Maximum pulse width | 2400 μs (default) |

**উদাহরণ:**
```cpp
myServo.attach(9);           // সাধারণ
myServo.attach(9, 1000, 2000); // কাস্টম range
```

---

#### ৪. Forward Sweep (সামনের দিকে ঘোরা)

```cpp
for (int angle = 0; angle <= 180; angle += 1) {
  myServo.write(angle);
  delay(15);
}
```

**কী করে?**
- `angle` ভেরিয়েবল 0 থেকে শুরু করে 180 পর্যন্ত যায়
- প্রতিটি ধাপে angle 1° বাড়ে
- সার্ভো সেই angle-এ চলে যায়
- 15ms অপেক্ষা করে যাতে সার্ভো পৌঁছাতে পারে

**ধাপে ধাপে:**

| ধাপ | angle | সার্ভোর অবস্থান | সময় |
|-----|-------|-----------------|------|
| 1 | 0 | 0° (সম্পূর্ণ বামে) | 0ms |
| 2 | 1 | 1° | 15ms |
| 3 | 2 | 2° | 30ms |
| ... | ... | ... | ... |
| 90 | 89 | 89° | 1335ms |
| 91 | 90 | 90° (মাঝে) | 1350ms |
| ... | ... | ... | ... |
| 180 | 179 | 179° | 2685ms |
| 181 | 180 | 180° (সম্পূর্ণ ডানে) | 2700ms |

**মোট সময়:** 181 × 15ms = **2.715 সেকেন্ড**

---

#### ৫. `write()` ফাংশন

```cpp
myServo.write(angle);
```

**কী করে?**
- সার্ভো মোটরকে নির্দিষ্ট angle-এ পাঠায়
- 0 থেকে 180 এর মধ্যে যেকোনো সংখ্যা দেওয়া যায়

**PWM ম্যাপিং:**

| Angle (°) | Pulse Width (ms) | ব্যাখ্যা |
|----------|------------------|---------|
| 0 | 1.0 | সম্পূর্ণ বামে |
| 45 | 1.25 | বাম থেকে ¼ ডানে |
| 90 | 1.5 | মাঝখানে |
| 135 | 1.75 | ডান থেকে ¼ বামে |
| 180 | 2.0 | সম্পূর্ণ ডানে |

**PWM সিগন্যাল দেখতে:**
```
0° অবস্থান (1ms pulse):
|▄____________________| 20ms period
 1ms HIGH

90° অবস্থান (1.5ms pulse):
|▄▄▄___________________| 20ms period
 1.5ms HIGH

180° অবস্থান (2ms pulse):
|▄▄▄▄__________________| 20ms period
 2ms HIGH
```

---

#### ৬. Delay কেন দরকার?

```cpp
delay(15);
```

**কারণ:**
- সার্ভো মোটর তাৎক্ষণিক নতুন angle-এ যেতে পারে না
- Physical movement এর জন্য সময় লাগে
- Delay ছাড়া Arduino পরবর্তী command পাঠিয়ে দেয়

**বিভিন্ন delay এর প্রভাব:**

| Delay (ms) | প্রভাব | ব্যবহার |
|-----------|--------|---------|
| 0-5 | খুব দ্রুত, সার্ভো follow করতে পারে না | ❌ সুপারিশ নয় |
| 10-20 | Smooth এবং দ্রুত | ✅ আদর্শ |
| 50-100 | মাঝারি গতি | ✅ stable |
| 500+ | খুব ধীর, step-by-step দেখা যায় | Debugging এর জন্য |

**উদাহরণ:**
```cpp
delay(5);   // খুব দ্রুত, জিটার হতে পারে
delay(15);  // Perfect smooth motion
delay(100); // ধীর কিন্তু stable
```

---

#### ৭. Reverse Sweep (পেছনে ফিরে আসা)

```cpp
for (int angle = 180; angle >= 0; angle -= 1) {
  myServo.write(angle);
  delay(15);
}
```

**কী করে?**
- `angle` ভেরিয়েবল 180 থেকে শুরু করে 0 পর্যন্ত কমে
- প্রতিটি ধাপে angle 1° কমে
- সার্ভো আবার 0°-এ ফিরে আসে

**ধাপে ধাপে:**

| ধাপ | angle | সার্ভোর অবস্থান | সময় |
|-----|-------|-----------------|------|
| 1 | 180 | 180° (ডানে) | 0ms |
| 2 | 179 | 179° | 15ms |
| ... | ... | ... | ... |
| 90 | 91 | 91° | 1335ms |
| 91 | 90 | 90° (মাঝে) | 1350ms |
| ... | ... | ... | ... |
| 180 | 1 | 1° | 2685ms |
| 181 | 0 | 0° (বামে) | 2700ms |

**মোট সময়:** 181 × 15ms = **2.715 সেকেন্ড**

---

### 🔄 Loop এর মোট সময়:

```
Forward sweep:  2.715 সেকেন্ড
Reverse sweep:  2.715 সেকেন্ড
──────────────────────────────
মোট:           5.43 সেকেন্ড

তারপর আবার শুরু (loop)
```

---

### 📊 গতি বিশ্লেষণ:

```
সার্ভো গতির গ্রাফ:

Angle
180°├────────╮           ╭────────╮
    │        │           │        │
    │        │           │        │
 90°│        │           │        │
    │        │           │        │
    │        ╰───────────╯        ╰──►
  0°└──────────────────────────────────► Time
    0s      2.7s       5.4s     8.1s

সময় অনুযায়ী:
0.0s - 2.7s: 0° → 180° (Forward)
2.7s - 5.4s: 180° → 0° (Reverse)
5.4s - 8.1s: 0° → 180° (Forward)
```

---

## ⚙️ কীভাবে কাজ করে

### সার্ভো মোটরের নিয়ন্ত্রণ পদ্ধতি:

```
Arduino → PWM সিগন্যাল পাঠায়
         ↓
সার্ভোর কন্ট্রোল সার্কিট → সিগন্যাল receive করে
         ↓
পটেনশিওমিটার থেকে → বর্তমান position check করে
         ↓
Error হিসাব করে → (যেখানে যেতে হবে - বর্তমান position)
         ↓
DC মোটর → সঠিক দিকে ঘোরে
         ↓
গিয়ার সিস্টেম → শক্তি বাড়ায়, গতি কমায়
         ↓
আউটপুট শ্যাফট → নির্দিষ্ট angle-এ পৌঁছায়
         ↓
পটেনশিওমিটার → confirm করে position
         ↓
মোটর → বন্ধ হয় (position ধরে রাখে)
```

### PWM এবং Angle এর সম্পর্ক:

```
Pulse Width → Angle নির্ধারণ করে:

500μs   → সার্ভো ভিন্ন হতে পারে
1000μs  → 0° (সম্পূর্ণ বামে)
1100μs  → 18°
1200μs  → 36°
1300μs  → 54°
1400μs  → 72°
1500μs  → 90° (মাঝখানে)
1600μs  → 108°
1700μs  → 126°
1800μs  → 144°
1900μs  → 162°
2000μs  → 180° (সম্পূর্ণ ডানে)
2500μs  → সার্ভো ভিন্ন হতে পারে

Frequency: 50 Hz (প্রতি 20ms-এ একটি pulse)
```

### ব্যবহারিক উদাহরণ:

```cpp
// নির্দিষ্ট angle-এ যাওয়া:
myServo.write(0);    // বামে
delay(1000);         // 1 সেকেন্ড থামুন
myServo.write(90);   // মাঝে
delay(1000);
myServo.write(180);  // ডানে
delay(1000);
```

---

## 🔧 সমস্যা ও সমাধান

### সাধারণ সমস্যা এবং সমাধান:

| সমস্যা | সম্ভাব্য কারণ | সমাধান |
|--------|--------------|---------|
| সার্ভো নড়ছে না | তার ভুল জায়গায় | চেক করুন: লাল=5V, বাদামী=GND, কমলা=D9 |
| কাঁপছে (jitter) | যথেষ্ট পাওয়ার নেই | External 5V power supply ব্যবহার করুন |
| একটা জায়গায় আটকে আছে | Library include হয়নি | `#include <Servo.h>` যুক্ত করুন |
| Random movement | Signal তার loose | D9-এর সাথে ভালোভাবে connect করুন |
| গরম হচ্ছে | Motor stall হয়েছে | বাধা সরান, load কমান |
| 180° ঘুরছে না | সার্ভো limited range | কিছু সার্ভো <180° হয় |
| Arduino reset হচ্ছে | Power সমস্যা | External power দিন |

---

### পাওয়ার সমস্যা চেনার উপায়:

**Insufficient Power এর লক্ষণ:**
- ✗ সার্ভো কাঁপছে বা ঝাঁকুনি দিচ্ছে
- ✗ Arduino randomly reset হচ্ছে
- ✗ Serial monitor disconnect হয়ে যাচ্ছে
- ✗ LED dim হয়ে যায় যখন সার্ভো নড়ে

**সমাধান:**

```
১টি সার্ভো:
  → Arduino 5V সাধারণত যথেষ্ট

২-৩টি সার্ভো:
  → External 5V 1A power supply

৪+ সার্ভো:
  → External 5V 2A+ power supply

External Power সংযোগ:
  ┌─────────────────────────────┐
  │ 5V Supply (+) → Servo Red   │
  │ 5V Supply (-) → Servo Brown │
  │                 Arduino GND │
  │                 (common!)   │
  │ Arduino D9 → Servo Signal   │
  └─────────────────────────────┘
  
⚠️ Common ground অবশ্যই দিতে হবে!
```

---

### Debugging কোড:

সমস্যা খুঁজতে এই কোড ব্যবহার করুন:

```cpp
#include <Servo.h>

Servo myServo;

void setup() {
  Serial.begin(9600);
  myServo.attach(9);
  Serial.println("সার্ভো pin 9-এ সংযুক্ত");
}

void loop() {
  for (int angle = 0; angle <= 180; angle += 10) {
    myServo.write(angle);
    Serial.print("কোণ: ");
    Serial.println(angle);
    delay(500);  // প্রতিটি step দেখার জন্য
  }
  
  Serial.println("180° থেকে ফিরছি...");
  
  for (int angle = 180; angle >= 0; angle -= 10) {
    myServo.write(angle);
    Serial.print("কোণ: ");
    Serial.println(angle);
    delay(500);
  }
}
```

**Serial Monitor Output দেখবেন:**
```
সার্ভো pin 9-এ সংযুক্ত
কোণ: 0
কোণ: 10
কোণ: 20
...
কোণ: 180
180° থেকে ফিরছি...
কোণ: 180
কোণ: 170
...
```

---

## 🎓 শিক্ষণীয় বিষয়

### এই প্রজেক্ট থেকে যা শিখলাম:

| ধারণা | ব্যাখ্যা | বাস্তব ব্যবহার |
|------|---------|----------------|
| **সার্ভো মোটর** | নির্দিষ্ট কোণে ঘোরে | রোবট, RC গাড়ি, automation |
| **PWM সিগন্যাল** | Pulse width দিয়ে নিয়ন্ত্রণ | Motor control, LED dimming |
| **Servo Library** | জটিল code সহজ করে | দ্রুত development |
| **Mechanical Control** | Digital → Physical motion | Actuators, robotic arms |
| **Feedback System** | Position check করে | নির্ভুলতা বাড়ায় |

---

### 🚀 এই প্রজেক্ট বাড়ানোর আইডিয়া:

#### ১. **Potentiometer দিয়ে Control**
```cpp
int potPin = A0;
int angle = map(analogRead(potPin), 0, 1023, 0, 180);
myServo.write(angle);
```

#### ২. **একাধিক সার্ভো একসাথে**
```cpp
Servo servo1, servo2, servo3;
servo1.attach(9);
servo2.attach(10);
servo3.attach(11);
```

#### ৩. **Ultrasonic Sensor দিয়ে Control**
- দূরত্ব অনুযায়ী সার্ভো নড়ানো
- Object tracking system

#### ৪. **Robotic Arm তৈরি**
- ৪-৬টি সার্ভো দিয়ে arm
- Gripper mechanism

#### ৫. **Pan-Tilt Camera Mount**
- 2-axis camera positioning
- Security camera system

---

### 🌟 বাস্তব জীবনে ব্যবহার:

- 🤖 **রোবট** - আর্ম, গ্রিপার, জয়েন্ট
- 📷 **ক্যামেরা** - Gimbal, stabilizer
- 🚪 **স্মার্ট ডোর** - Automatic door lock
- ✈️ **RC বিমান** - Control surfaces
- 🚗 **RC গাড়ি** - Steering
- 🏭 **শিল্প** - নির্ভুল positioning

---

## 📚 আরও শিখুন:

### প্রশ্ন-উত্তর:

**প্রশ্ন ১:** সার্ভো কি ক্রমাগত ঘুরতে পারে?  
**উত্তর:** না, স্ট্যান্ডার্ড সার্ভো শুধু 0-180° ঘোরে। Continuous rotation servo ক্রমাগত ঘোরে।

**প্রশ্ন ২:** কেন PWM pin দরকার?  
**উত্তর:** PWM pin সঠিক timing-এ signal পাঠাতে পারে। সার্ভো এই timing দেখে angle বোঝে।

**প্রশ্ন ৩:** একসাথে কতগুলো সার্ভো চালানো যায়?  
**উত্তর:** Arduino UNO তে 12টি পর্যন্ত (software PWM দিয়ে)। কিন্তু power সমস্যার কারণে external supply দরকার।

**প্রশ্ন ৪:** Delay কমালে কী হবে?  
**উত্তর:** সার্ভো দ্রুত নড়বে কিন্তু jerky motion হতে পারে।

---

## ✅ Checklist

### প্রজেক্ট শুরুর আগে:

- [ ] সার্ভোর তারের রঙ চিহ্নিত করেছি
- [ ] Arduino IDE-তে Servo library আছে কিনা check করেছি
- [ ] সব জাম্পার তার প্রস্তুত আছে
- [ ] Power supply যথেষ্ট কিনা নিশ্চিত হয়েছি

### সংযোগের সময়:

- [ ] লাল তার → 5V
- [ ] বাদামী/কালো তার → GND
- [ ] কমলা/হলুদ তার → D9
- [ ] সব সংযোগ শক্ত করে লাগানো

### Code upload এর পর:

- [ ] সার্ভো 0° থেকে 180° যাচ্ছে
- [ ] আবার 180° থেকে 0° ফিরছে
- [ ] Motion smooth এবং jerky নয়
- [ ] কোনো অস্বাভাবিক শব্দ নেই

---

## 👨‍🎓 লেখক

**Md. Akhinoor Islam**  
📚 Energy Science and Engineering (ESE)  
🏫 Khulna University of Engineering & Technology (KUET)  
🌐 [GitHub Profile](https://github.com/Akhinoor14)

---

## 🎯 Challenge Levels

### 🟢 Beginner:
- [ ] Servo শুধু 0° এবং 90° মধ্যে নড়ান
- [ ] Delay 50ms করে slow motion তৈরি করুন

### 🟡 Intermediate:
- [ ] Potentiometer দিয়ে manual control যোগ করুন
- [ ] Button দিয়ে specific angles (0°, 45°, 90°, 135°, 180°) set করুন

### 🔴 Advanced:
- [ ] 2টি সার্ভো দিয়ে pan-tilt system বানান
- [ ] Ultrasonic sensor এর distance অনুযায়ী servo নড়ান
- [ ] 4-servo robotic arm তৈরি করুন

---

## 📖 শব্দকোষ:

| শব্দ | বাংলা | ব্যাখ্যা |
|-----|------|---------|
| Servo | সার্ভো | নির্দিষ্ট কোণে ঘোরার মোটর |
| PWM | পালস উইড্থ মডুলেশন | Signal এর width দিয়ে control |
| Angle | কোণ | ঘূর্ণনের পরিমাপ (degree) |
| Sweep | ঝাড়ু দেওয়া | এক প্রান্ত থেকে অন্য প্রান্তে যাওয়া |
| Torque | টর্ক | ঘোরানোর শক্তি |
| Stall | আটকে যাওয়া | মোটর ঘুরতে না পারা |

---

### 🔗 সম্পর্কিত প্রজেক্ট:

1. [LED Pattern](../01%20LED%20Pattern/)
2. [Breathing LED](../03%20Breathing%20a%20LED/)
3. [RGB LED Control](../05%20RGB%20LED%20Control%20with%20PWM/)
4. [Ultrasonic Sensor](../07%20Interfacing%20an%20ultrasonic%20sensor%20with%20arduino/)

---

**শুভ কোডিং! 🎉**  
**সার্ভো মোটর দিয়ে অসাধারণ কিছু বানান! 🤖**
