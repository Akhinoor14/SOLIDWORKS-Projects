# 🎲 ইলেকট্রনিক পাশা - 7-Segment Display দিয়ে

![ATtiny85](https://img.shields.io/badge/MCU-ATtiny85-blue?style=for-the-badge)
![74HC595](https://img.shields.io/badge/IC-74HC595%20Shift%20Register-green?style=for-the-badge)
![7-Segment](https://img.shields.io/badge/Display-7--Segment%20CC-red?style=for-the-badge)
![বাংলা](https://img.shields.io/badge/ভাষা-বাংলা-orange?style=for-the-badge)

---

## 📚 সূচিপত্র
- [প্রজেক্ট পরিচিতি](#-প্রজেক্ট-পরিচিতি)
- [যা যা লাগবে](#-যা-যা-লাগবে)
- [7-Segment Display কী](#-7-segment-display-কী)
- [74HC595 Shift Register কী](#-74hc595-shift-register-কী)
- [সার্কিট সংযোগ](#-সার্কিট-সংযোগ)
- [পিন ম্যাপিং](#-পিন-ম্যাপিং)
- [কাজের নিয়ম](#-কাজের-নিয়ম)
- [কোড ব্যাখ্যা](#-কোড-ব্যাখ্যা)
- [সমস্যা সমাধান](#-সমস্যা-সমাধান)
- [নিজে চেষ্টা করো](#-নিজে-চেষ্টা-করো)

---

## 🎯 প্রজেক্ট পরিচিতি

এই প্রজেক্টে আমরা **ATtiny85 মাইক্রোকন্ট্রোলার**, **74HC595 shift register**, এবং **7-segment display** ব্যবহার করে একটি **ডিজিটাল পাশা** (dice) বানাবো। বোতাম চাপলে পাশা ঘুরবে এবং ১ থেকে ৬ পর্যন্ত যেকোনো একটি নাম্বার দেখাবে!

### 🌟 কেন এই প্রজেক্ট বিশেষ?

```
✅ মাত্র ৩টা পিন দিয়ে ৭টা সেগমেন্ট কন্ট্রোল
✅ সুন্দর animation দেখায় পাশা ঘোরার সময়
✅ সত্যিকারের random number তৈরি হয়
✅ খুবই কম জায়গা নেয় (ATtiny85 মাত্র ৮-পিন!)
✅ কম বিদ্যুৎ খরচ (~20mA)
✅ বোর্ড গেমে ব্যবহার করা যায়
```

---

## 🧰 যা যা লাগবে

### প্রয়োজনীয় যন্ত্রাংশ:

| যন্ত্রাংশ | স্পেসিফিকেশন | সংখ্যা | কাজ |
|-----------|--------------|--------|------|
| **ATtiny85** | 8-pin DIP, 8KB Flash | ১টি | মূল মাইক্রোকন্ট্রোলার |
| **74HC595** | 8-bit shift register | ১টি | Serial থেকে parallel রূপান্তর |
| **7-Segment Display** | Common Cathode, লাল/সবুজ | ১টি | ০-৬ নাম্বার দেখানোর জন্য |
| **Push Button** | Tactile switch | ১টি | পাশা রোল করার জন্য |
| **Resistor** | 330Ω, 1/4W | ৮টি | Current limiting (৭টা সেগমেন্ট + ১টা cathode) |
| **Resistor** | 10kΩ, 1/4W | ১টি | Button pull-down (optional) |
| **LED** | 5mm, যেকোনো রঙ | ১টি | Indicator (optional) |
| **Breadboard** | Half-size বা full | ১টি | Prototyping |
| **Jumper Wires** | Male-to-Male | ২০+ | সংযোগের জন্য |
| **Power Supply** | 3V-5V (USB বা battery) | ১টি | Power source |

### মোট খরচ: প্রায় ৩০০-৫০০ টাকা

---

## 🔬 7-Segment Display কী?

### 7-Segment Display বুঝি:

**7-segment display** হলো ৭টা LED এর সমন্বয়ে তৈরি একটা ডিসপ্লে যেটা দিয়ে সংখ্যা দেখানো যায়।

```
7-Segment এর গঠন:
      ┌─── a ───┐
      │         │
      f         b
      │         │
      ├─── g ───┤
      │         │
      e         c
      │         │
      └─── d ───┘  ● dp (decimal point)

সেগমেন্টের নাম:
  a = উপরের অংশ
  b = ডান উপরের অংশ
  c = ডান নিচের অংশ
  d = নিচের অংশ
  e = বাম নিচের অংশ
  f = বাম উপরের অংশ
  g = মাঝের অংশ
  dp = decimal point (এই প্রজেক্টে ব্যবহার হয়নি)
```

### Common Cathode vs Common Anode:

```
Common Cathode (CC):
  • সব cathode (─) একসাথে GND এ যুক্ত
  • Anode (+) দিয়ে আলাদা আলাদা segment control করা হয়
  • HIGH = segment জ্বলবে ✅
  • LOW = segment নিভবে
  
  সংযোগ:
    ATtiny → 330Ω → Segment anode
    Segment cathode → GND (common)

Common Anode (CA):
  • সব anode (+) একসাথে VCC তে যুক্ত
  • Cathode (─) দিয়ে আলাদা আলাদা segment control করা হয়
  • LOW = segment জ্বলবে ✅
  • HIGH = segment নিভবে
  
  সংযোগ:
    Segment anode → VCC (common)
    ATtiny → 330Ω → Segment cathode

এই প্রজেক্টে Common Cathode (CC) ব্যবহার করা হয়েছে!
```

### নাম্বার দেখানোর Pattern:

```
কোন কোন সেগমেন্ট জ্বলবে:

সংখ্যা ০: a,b,c,d,e,f জ্বলবে (g নিভবে)
  ┌─────┐
  │     │
  │     │
  │     │
  │     │
  │     │
  └─────┘

সংখ্যা ১: শুধু b,c জ্বলবে
  ┌─────┐
        │
        │
  ──────┤
        │
        │
  └─────┘

সংখ্যা ২: a,b,g,e,d জ্বলবে
  ┌─────┐
        │
  ──────┤
  │
  │
  └─────┘

সংখ্যা ৩: a,b,g,c,d জ্বলবে
  ┌─────┐
        │
  ──────┤
        │
        │
  └─────┘

সংখ্যা ৪: f,g,b,c জ্বলবে
  ┌─────┐
  │     │
  ──────┤
        │
        │
  └─────┘

সংখ্যা ৫: a,f,g,c,d জ্বলবে
  ┌─────┐
  │
  ──────┤
        │
        │
  └─────┘

সংখ্যা ৬: a,f,g,e,d,c জ্বলবে (সব কিছু ছাড়া b)
  ┌─────┐
  │
  ──────┤
  │     │
  │     │
  └─────┘
```

### Binary Pattern (বাইনারি প্যাটার্ন):

```
gfedcba format (g=MSB, a=LSB):

সংখ্যা │ Binary    │ Hex  │ যে সেগমেন্টগুলো জ্বলবে
──────┼───────────┼──────┼─────────────────────
  ০   │ 0x3F      │ 0x3F │ a,b,c,d,e,f
  ১   │ 0x06      │ 0x06 │ b,c
  ২   │ 0x5B      │ 0x5B │ a,b,g,e,d
  ৩   │ 0x4F      │ 0x4F │ a,b,g,c,d
  ৪   │ 0x66      │ 0x66 │ f,g,b,c
  ৫   │ 0x6D      │ 0x6D │ a,f,g,c,d
  ৬   │ 0x7D      │ 0x7D │ a,f,g,e,d,c
  ৭   │ 0x07      │ 0x07 │ a,b,c
  ৮   │ 0x7F      │ 0x7F │ সব সেগমেন্ট
  ৯   │ 0x6F      │ 0x6F │ a,b,c,d,f,g
 OFF  │ 0x00      │ 0x00 │ কোনোটাই না

উদাহরণ: সংখ্যা ১ = 0x06 = 0b00000110
  Bit 0 (a): 0 = নিভবে
  Bit 1 (b): 1 = জ্বলবে ✅
  Bit 2 (c): 1 = জ্বলবে ✅
  Bit 3 (d): 0 = নিভবে
  Bit 4 (e): 0 = নিভবে
  Bit 5 (f): 0 = নিভবে
  Bit 6 (g): 0 = নিভবে
  → ফলাফল: শুধু b এবং c জ্বলবে = "১" দেখাবে!
```

---

## 🔀 74HC595 Shift Register কী?

### Shift Register কেন দরকার?

```
সমস্যা:
  ৭টা segment + ১টা cathode = ৮টা pin লাগবে ❌
  ATtiny85 এ মাত্র ৬টা I/O pin আছে!

সমাধান:
  74HC595 shift register ব্যবহার করে
  মাত্র ৩টা pin দিয়ে ৮টা output control করা যায় ✅
  
  ৩টা pin বাঁচে = অন্য কাজে ব্যবহার করা যাবে!
```

### 74HC595 কীভাবে কাজ করে?

**Shift Register** হলো একটা IC যেটা **serial data** (একটা একটা করে bit) নেয় এবং **parallel data** (একসাথে ৮টা bit) বের করে দেয়।

```
Serial-to-Parallel Conversion:

Step 1: Serial Data পাঠাও (একটা একটা করে bit)
  DS pin → bit আসে
  SH_CP pulse → shift করা হয়
  
  উদাহরণ: 0x06 (সংখ্যা ১) পাঠাতে হবে:
    Bit 7: 0 → DS=LOW, pulse SH_CP
    Bit 6: 0 → DS=LOW, pulse SH_CP
    Bit 5: 0 → DS=LOW, pulse SH_CP
    Bit 4: 0 → DS=LOW, pulse SH_CP
    Bit 3: 0 → DS=LOW, pulse SH_CP
    Bit 2: 1 → DS=HIGH, pulse SH_CP ✅
    Bit 1: 1 → DS=HIGH, pulse SH_CP ✅
    Bit 0: 0 → DS=LOW, pulse SH_CP
    
Step 2: Latch করো (output update করো)
  ST_CP pin → LOW থেকে HIGH করো
  → সব output একসাথে update হয়ে যাবে!
  → QA-QH এ নতুন data আসবে
```

### 74HC595 Pinout (DIP-16):

```
74HC595 IC এর পিন:
        ┌─────────────┐
    QB  │1    ●    16│ VCC (+5V)
    QC  │2        15│ QA
    QD  │3        14│ DS (Serial Data)
    QE  │4        13│ OE (Output Enable)
    QF  │5        12│ ST_CP (Latch Clock)
    QG  │6        11│ SH_CP (Shift Clock)
    QH  │7        10│ MR (Master Reset)
   GND  │8         9│ QH' (Serial Out)
        └─────────────┘

পিনের কাজ:
  QA-QH (15,1-7): Parallel outputs (7-seg segments a-g তে যাবে)
  DS (14): Serial data input (ATtiny PB4 থেকে আসবে)
  SH_CP (11): Shift clock (ATtiny PB2 থেকে আসবে)
  ST_CP (12): Latch clock (ATtiny PB3 থেকে আসবে)
  OE (13): Output enable (GND এ রাখো = always ON)
  MR (10): Master reset (VCC তে রাখো = no reset)
  QH' (9): Serial output (একাধিক 74HC595 chain করতে পারবে)
```

### Arduino shiftOut() Function:

```cpp
shiftOut(dataPin, clockPin, MSBFIRST, value);
```

**এই function কী করে:**

```
১. dataPin (DS) এ প্রথম bit পাঠায় (HIGH/LOW)
২. clockPin (SH_CP) একবার pulse দেয় (LOW → HIGH → LOW)
৩. আবার পরের bit পাঠায়
৪. এভাবে ৮টা bit পাঠানো হয়
৫. তোমাকে শেষে latch pin (ST_CP) manually pulse দিতে হবে

MSBFIRST = Most Significant Bit first (বড় bit আগে)
LSBFIRST = Least Significant Bit first (ছোট bit আগে)
```

**উদাহরণ:**

```cpp
digitalWrite(latchPin, LOW);  // ১. Latch LOW করো
shiftOut(dataPin, clockPin, MSBFIRST, 0x06);  // ২. Data পাঠাও
digitalWrite(latchPin, HIGH);  // ৩. Latch HIGH করো
// → ফলাফল: Display তে "১" দেখাবে!
```

---

## 🔌 সার্কিট সংযোগ

### সম্পূর্ণ সার্কিট:

```
ইলেকট্রনিক পাশা সার্কিট:
┌──────────────────────────────────────────────────────────────┐
│                                                              │
│  Push Button                                                 │
│    ┌────┐                                                    │
│    │    │                                                    │
│  ──┤    ├── VCC (+5V)                                        │
│    │    │                                                    │
│    └──┬─┘                                                    │
│       │                                                      │
│       ├──── ATtiny85 PB0 (Pin 5)                             │
│       │                                                      │
│      10kΩ (pull-down, optional)                              │
│       │                                                      │
│      GND                                                     │
│                                                              │
│         ATtiny85                    74HC595                  │
│      ┌────────┐                  ┌─────────────┐            │
│      │1  ●  8├─ VCC              │   74HC595   │            │
│ PB3 ─┤2     7├─ PB2              │             │            │
│(Latch)│      │ (Clock)        VCC│16        15│QA → a      │
│ PB4 ─┤3     6├─ PB1          QB  │1         14│DS ← PB4    │
│(Data) │      │ (Cathode)     QC  │2         13│OE → GND    │
│      │4     5├─ PB0 (Button) QD  │3         12│ST_CP←PB3   │
│      └──┬─────┘                QE │4         11│SH_CP←PB2   │
│      GND│ VCC                  QF │5         10│MR → VCC    │
│         │  │                   QG │6          9│QH' (NC)    │
│         │  │                   QH │7          8│GND         │
│         │  │                      └─────────────┘            │
│         │  │                        │  │  │  │  │  │  │     │
│         │  │                       QA QB QC QD QE QF QG     │
│         │  │                        │  │  │  │  │  │  │     │
│         │  │                       330Ω (×৭টা resistor)     │
│         │  │                        │  │  │  │  │  │  │     │
│         │  │         7-Segment Display (Common Cathode)     │
│         │  │      ┌──────────────────────────────┐          │
│         │  │      │       ┌─── a ───┐            │          │
│         │  │      │       │         │            │          │
│         │  │      │       f         b            │          │
│         │  │      │       │         │            │          │
│         │  │      │       ├─── g ───┤            │          │
│         │  │      │       │         │            │          │
│         │  │      │       e         c            │          │
│         │  │      │       │         │            │          │
│         │  │      │       └─── d ───┘            │          │
│         │  │      │                              │          │
│         │  │      │  Pins: a b c d e f g         │          │
│         │  │      └───┬──┬──┬──┬──┬──┬──┬───────┘          │
│         │  │          │  │  │  │  │  │  │                  │
│         │  │         QA QB QC QD QE QF QG (74HC595 থেকে)   │
│         │  │                              │                 │
│         │  │                    Common Cathode              │
│         │  │                              │                 │
│         │  └──────────────────────────── 330Ω              │
│         │                                 │                 │
│         │                            ATtiny PB1             │
│         │                                                   │
│        GND ──────────────────────────────────────────────  │
│                                                              │
│        VCC (+5V) ────────────────────────────────────────  │
└──────────────────────────────────────────────────────────────┘

Power Supply:
  • USB 5V অথবা ৩টা AA battery (4.5V)
  • 0.1µF capacitor (optional, IC এর কাছে)
```

---

## 📍 পিন ম্যাপিং

### ATtiny85 এর সংযোগ:

| ATtiny85 | Physical Pin | কাজ | যুক্ত হবে | Signal Type |
|----------|-------------|-----|-----------|-------------|
| **RESET** | Pin 1 | Reset (pull-up) | সংযুক্ত নয় | - |
| **PB3** | Pin 2 | Output | 74HC595 Pin 12 (ST_CP) | Latch clock |
| **PB4** | Pin 3 | Output | 74HC595 Pin 14 (DS) | Serial data |
| **GND** | Pin 4 | Ground | Common GND | Power |
| **PB0** | Pin 5 | Input | Push button + 10kΩ pull-down | Digital input |
| **PB1** | Pin 6 | Output | 7-seg cathode via 330Ω | Display enable |
| **PB2** | Pin 7 | Output | 74HC595 Pin 11 (SH_CP) | Shift clock |
| **VCC** | Pin 8 | Power | +5V | Power |

### 74HC595 থেকে 7-Segment এর সংযোগ:

| 74HC595 Output | Pin | 7-Segment | Segment Name |
|----------------|-----|-----------|--------------|
| QA | 15 | a | উপরের অংশ |
| QB | 1 | b | ডান উপরের অংশ |
| QC | 2 | c | ডান নিচের অংশ |
| QD | 3 | d | নিচের অংশ |
| QE | 4 | e | বাম নিচের অংশ |
| QF | 5 | f | বাম উপরের অংশ |
| QG | 6 | g | মাঝের অংশ |
| QH | 7 | (ব্যবহার হয়নি) | - |

### Code-এ Pin Definition:

```cpp
কোড ভেরিয়েবল        ATtiny85 Pin    সংযুক্ত
────────────────────────────────────────────────
const int buttonPin = 0;  PB0 (Pin 5)  → Button
const int ledPin = 1;     PB1 (Pin 6)  → Cathode
const int clockPin = 2;   PB2 (Pin 7)  → SH_CP
const int latchPin = 3;   PB3 (Pin 2)  → ST_CP
const int dataPin = 4;    PB4 (Pin 3)  → DS
```

---

## ⚙️ কাজের নিয়ম

### সিস্টেম কীভাবে চলে:

```
┌────────────────────────────────────────────────┐
│           পাশা সিস্টেম অপারেশন                │
└────────────────────────────────────────────────┘
                    │
              Power ON
                    │
                    ▼
         ┌──────────────────┐
         │ ATtiny85 Setup   │
         │ • Pin setup      │
         │ • Display init   │
         └─────────┬────────┘
                   │
                   ▼
         ┌──────────────────┐
         │ Welcome Animation│
         │ • "8" দেখাও      │
         │ • 3 sec delay    │
         │ • Blank দেখাও    │
         │ • "0" দেখাও      │
         └─────────┬────────┘
                   │
                   ▼
         ┌──────────────────┐
         │ Button এর জন্য   │ ◄──────────┐
         │ অপেক্ষা করো      │            │
         │ digitalRead(PB0) │            │
         └─────────┬────────┘            │
                   │                     │
            না ←───┴───→ হ্যাঁ           │
            │           │                │
            │           ▼                │
            │  ┌──────────────────┐     │
            │  │ Button চাপা হয়েছে│     │
            │  └─────────┬────────┘     │
            │            │               │
            │            ▼               │
            │  ┌──────────────────┐     │
            │  │ Display OFF      │     │
            │  │ LEDwrite(7)      │     │
            │  └─────────┬────────┘     │
            │            │               │
            │            ▼               │
            │  ┌──────────────────┐     │
            │  │ Random নাম্বার   │     │
            │  │ তৈরি করো (১-৬)  │     │
            │  │ RND() function   │     │
            │  └─────────┬────────┘     │
            │            │               │
            │            ▼               │
            │  ┌──────────────────┐     │
            │  │ Roll Animation   │     │
            │  │ rollDice(2)      │     │
            │  │ ০→১→২→৩→৪→৫→৬  │     │
            │  │ (২ বার repeat)   │     │
            │  └─────────┬────────┘     │
            │            │               │
            │            ▼               │
            │  ┌──────────────────┐     │
            │  │ Display OFF      │     │
            │  │ (একটু বিরতি)     │     │
            │  └─────────┬────────┘     │
            │            │               │
            │            ▼               │
            │  ┌──────────────────┐     │
            │  │ ফলাফল দেখাও     │     │
            │  │ LEDwrite(digit)  │     │
            │  │ (১-৬ দেখাবে)     │     │
            │  └─────────┬────────┘     │
            │            │               │
            └────────────┼───────────────┘
                         │
                         └─────────────────┘
                    (পরের চাপের জন্য অপেক্ষা)
```

---

## 💻 কোড ব্যাখ্যা

### সম্পূর্ণ কোড:

```cpp
/*
 * Project 16: ইলেকট্রনিক পাশা
 * ATtiny85 + 74HC595 + 7-Segment Display
 */

#define segDISPLAY CATHODE  // Common Cathode type
#define d250 delay(250);    // Delay macro

// Pin definitions
const int buttonPin = 0;  // PB0 (Pin 5) - বোতাম
const int ledPin = 1;     // PB1 (Pin 6) - Cathode control
const int clockPin = 2;   // PB2 (Pin 7) → SH_CP
const int latchPin = 3;   // PB3 (Pin 2) → ST_CP
const int dataPin = 4;    // PB4 (Pin 3) → DS

// 7-segment patterns (gfedcba)
const byte digit[] = {
  B00111111, // 0
  B00000110, // 1
  B01011011, // 2
  B01001111, // 3
  B01100110, // 4
  B01101101, // 5
  B01111101, // 6
  B00000000, // OFF (blank)
  B01111111  // 8 (সব segment)
};

void setup() {
  // সব pin setup করো
  pinMode(dataPin, OUTPUT);
  pinMode(clockPin, OUTPUT);
  pinMode(latchPin, OUTPUT);
  pinMode(buttonPin, INPUT);
  pinMode(ledPin, OUTPUT);

  // Display type অনুযায়ী cathode control
  #ifdef segDISPLAY
    digitalWrite(ledPin, LOW);   // CC: cathode to GND
  #else
    digitalWrite(ledPin, HIGH);  // CA: anode to VCC
  #endif

  // Welcome animation
  LEDwrite(8);  // সব segment জ্বালাও
  delay(3000);  // ৩ সেকেন্ড
  LEDwrite(7);  // OFF
  d250;
  LEDwrite(0);  // ০ দেখাও
}

void LEDwrite(int data) {
  digitalWrite(latchPin, LOW);  // Latch LOW
  
  #ifdef segDISPLAY
    shiftOut(dataPin, clockPin, MSBFIRST, digit[data]);
  #else
    shiftOut(dataPin, clockPin, MSBFIRST, ~digit[data]);
  #endif
  
  digitalWrite(latchPin, HIGH);  // Latch HIGH → Update!
}

int RND() {
  int seed = 0;
  int digit = 0;
  // ১-৬ না আসা পর্যন্ত চালাও
  while(digit > 6 || digit <= 0) {
    seed = (seed * 53) + 21;  // Pseudo-random formula
    digit = seed % 6;
    randomSeed(analogRead(PB5));  // Analog noise থেকে seed
    seed = random(50) + digit;
    digit += seed;
  }
  return digit;  // ১-৬ return করবে
}

void rollDice(int times) {
  // Animation: ০→১→২→৩→৪→৫→৬
  for (int i = 0; i < times; i++) {
    LEDwrite(0); d250;
    LEDwrite(1); d250;
    LEDwrite(2); d250;
    LEDwrite(3); d250;
    LEDwrite(4); d250;
    LEDwrite(5); d250;
    LEDwrite(6); d250;
  }
}

void loop() {
  int btn = digitalRead(buttonPin);
  if (btn == HIGH) {  // বোতাম চাপা হলে
    LEDwrite(7);  // Display OFF
    d250;
    int digit = RND();  // Random নাম্বার পাও
    rollDice(2);  // Animation (২ বার)
    LEDwrite(7);  // OFF
    d250;
    LEDwrite(digit);  // ফলাফল দেখাও
  }
}
```

---

### ধাপে ধাপে কোড বিশ্লেষণ:

#### **১. Preprocessor Directives (প্রাথমিক সংজ্ঞা):**

```cpp
#define segDISPLAY CATHODE
#define d250 delay(250);
```

**ব্যাখ্যা:**
- `segDISPLAY CATHODE`: Display এর type define করা (Common Cathode)
  - যদি Common Anode হয়, এই line মুছে ফেলবে
- `d250`: `delay(250)` এর shortcut (টাইপ করার সময় বাঁচে)

#### **২. Pin Definitions:**

```cpp
const int buttonPin = 0;  // PB0
const int ledPin = 1;     // PB1
const int clockPin = 2;   // PB2
const int latchPin = 3;   // PB3
const int dataPin = 4;    // PB4
```

**প্রতিটা পিনের কাজ:**
- `buttonPin`: বোতাম চাপা হয়েছে কিনা detect করে
- `ledPin`: Display এর cathode control করে (ON/OFF)
- `clockPin`: Shift clock (SH_CP) - 74HC595 এর জন্য
- `latchPin`: Latch clock (ST_CP) - 74HC595 এর জন্য
- `dataPin`: Serial data (DS) - 74HC595 এর জন্য

#### **৩. Segment Patterns Array:**

```cpp
const byte digit[] = {
  B00111111, // 0
  B00000110, // 1
  B01011011, // 2
  // ... ইত্যাদি
};
```

**Binary Format ব্যাখ্যা:**

```
B00000110 = সংখ্যা "১"
  ║║║║║║║║
  ║║║║║║║╚══ a (উপরের) = 0 (নিভবে)
  ║║║║║║╚═══ b (ডান উপরের) = 1 (জ্বলবে) ✓
  ║║║║║╚════ c (ডান নিচের) = 1 (জ্বলবে) ✓
  ║║║║╚═════ d (নিচের) = 0 (নিভবে)
  ║║║╚══════ e (বাম নিচের) = 0 (নিভবে)
  ║║╚═══════ f (বাম উপরের) = 0 (নিভবে)
  ║╚════════ g (মাঝের) = 0 (নিভবে)
  ╚═════════ (ব্যবহার হয়নি)

ফলাফল: শুধু b এবং c জ্বলবে → "১" দেখাবে!
```

#### **৪. setup() Function:**

```cpp
void setup() {
  // Pin setup
  pinMode(dataPin, OUTPUT);
  pinMode(clockPin, OUTPUT);
  pinMode(latchPin, OUTPUT);
  pinMode(buttonPin, INPUT);
  pinMode(ledPin, OUTPUT);

  // Cathode control
  #ifdef segDISPLAY
    digitalWrite(ledPin, LOW);
  #else
    digitalWrite(ledPin, HIGH);
  #endif

  // Welcome animation
  LEDwrite(8);  // সব segment জ্বালাও
  delay(3000);  // ৩ সেকেন্ড অপেক্ষা
  LEDwrite(7);  // সব নিভাও
  d250;
  LEDwrite(0);  // ০ দেখাও
}
```

**Initialization (শুরুর কাজ):**

1. সব pin INPUT/OUTPUT হিসেবে configure করো
2. Display type অনুযায়ী cathode pin set করো:
   - CC (Common Cathode): LOW (GND এ সংযুক্ত)
   - CA (Common Anode): HIGH (VCC তে সংযুক্ত)
3. Welcome animation:
   - "8" দেখাও (সব segment) ৩ সেকেন্ডের জন্য
   - Display blank করো
   - "0" দেখাও (ready state)

#### **৫. LEDwrite() Function:**

```cpp
void LEDwrite(int data) {
  digitalWrite(latchPin, LOW);  // ১. Latch LOW করো
  
  #ifdef segDISPLAY
    // Common Cathode: সরাসরি pattern
    shiftOut(dataPin, clockPin, MSBFIRST, digit[data]);
  #else
    // Common Anode: inverted pattern
    shiftOut(dataPin, clockPin, MSBFIRST, ~digit[data]);
  #endif
  
  digitalWrite(latchPin, HIGH);  // ৩. Latch HIGH করো
}
```

**এই function কী করে:**

```
ধাপ ১: Latch LOW করো
  → Output register আগের value hold করবে
  
ধাপ ২: Data পাঠাও (8 bits)
  → shiftOut() function ৮টা bit একে একে পাঠায়
  → MSBFIRST = Most Significant Bit first (বড় bit আগে)
  → digit[] array থেকে pattern নেয়
  
ধাপ ৩: Latch HIGH করো
  → Output register update হবে
  → 7-segment display তাৎক্ষণিক নতুন digit দেখাবে!

উদাহরণ: LEDwrite(1) call করলে
  1. Latch LOW
  2. Send: 0,0,0,0,0,1,1,0 (8 clock pulse)
  3. Latch HIGH → Display "১" দেখাবে ✅
```

#### **৬. RND() Function:**

```cpp
int RND() {
  int seed = 0;
  int digit = 0;
  while(digit > 6 || digit <= 0) {
    seed = (seed * 53) + 21;
    digit = seed % 6;
    randomSeed(analogRead(PB5));
    seed = random(50) + digit;
    digit += seed;
  }
  return digit;  // ১-৬ return করবে
}
```

**Random Number কীভাবে তৈরি হয়:**

```
সমস্যা:
  Software random() seed ছাড়া predictable!
  প্রতিবার একই sequence = random নয় ❌

সমাধান:
  Analog noise ব্যবহার করো
  Floating pin এর electrical noise = সত্যিকারের random ✅

প্রক্রিয়া:
  ১. PB5 (A1) floating রাখো → noise pick করবে
  ২. analogRead(PB5) → 0-1023 (random value)
  ৩. Seed তৈরি করো: randomSeed(noise_value)
  ৪. Number তৈরি করো:
     - seed = (seed * 53) + 21  (pseudo-random formula)
     - digit = seed % 6  (0-5 range)
     - random(50) যোগ করো
  ৫. Loop চালাও যতক্ষণ না ১ ≤ digit ≤ ৬
  ৬. digit return করো

প্রতিবার button চাপলে → ভিন্ন analog reading → ভিন্ন ফলাফল!

উদাহরণ readings (PB5 floating):
  চাপ ১: 342
  চাপ ২: 871
  চাপ ৩: 129
  প্রতিবার আলাদা!
```

#### **৭. rollDice() Function:**

```cpp
void rollDice(int times) {
  for (int i = 0; i < times; i++) {
    LEDwrite(0); d250;  // ০ দেখাও 250ms
    LEDwrite(1); d250;  // ১ দেখাও 250ms
    LEDwrite(2); d250;  // ২ দেখাও 250ms
    LEDwrite(3); d250;  // ৩ দেখাও 250ms
    LEDwrite(4); d250;  // ৪ দেখাও 250ms
    LEDwrite(5); d250;  // ৫ দেখাও 250ms
    LEDwrite(6); d250;  // ৬ দেখাও 250ms
  }
}
```

**Animation Sequence:**

```
rollDice(2) → ২ বার repeat হবে

Round 1:
  ০ → 250ms
  ১ → 250ms
  ২ → 250ms
  ৩ → 250ms
  ৪ → 250ms
  ৫ → 250ms
  ৬ → 250ms
  (মোট: 1.75 সেকেন্ড)

Round 2:
  ০ → 250ms
  ১ → 250ms
  ...
  ৬ → 250ms
  (মোট: 1.75 সেকেন্ড)

সর্বমোট animation: 3.5 সেকেন্ড
পাশা ঘোরার realistic effect দেয়!
```

**Visual Timeline:**

```
Time:     0ms  250  500  750  1000 1250 1500 1750 2000...
Display:   ০ → ১ → ২ → ৩ → ৪ → ৫ → ৬ → ০ → ১ ...
```

#### **৮. loop() Function:**

```cpp
void loop() {
  int btn = digitalRead(buttonPin);
  if (btn == HIGH) {
    LEDwrite(7);  // Display OFF
    d250;
    int digit = RND();  // Random number পাও
    rollDice(2);  // Animation
    LEDwrite(7);  // OFF আবার
    d250;
    LEDwrite(digit);  // ফলাফল দেখাও
  }
}
```

**Execution Flow:**

```
Infinite loop:
  ১. Button check করো: digitalRead(PB0)
  ২. যদি HIGH (চাপা হয়):
     a. Display OFF করো
     b. Random (১-৬) তৈরি করো
     c. Roll animation (২ cycle)
     d. Display OFF করো সংক্ষিপ্তভাবে
     e. Random ফলাফল দেখাও
  ৩. আবার ১ নম্বর step এ যাও

Button না চাপলে: শুধু check করতে থাকবে
```

---

## 🔧 Animation Customization:

### দ্রুত Animation (100ms প্রতি digit):

```cpp
#define d250 delay(100);
```

### ধীর Animation (500ms প্রতি digit):

```cpp
#define d250 delay(500);
```

### বেশি Round (৩ cycle):

```cpp
rollDice(3);
```

### Reverse Animation (৬→১):

```cpp
void rollDice(int times) {
  for (int i = 0; i < times; i++) {
    LEDwrite(6); d250;
    LEDwrite(5); d250;
    LEDwrite(4); d250;
    LEDwrite(3); d250;
    LEDwrite(2); d250;
    LEDwrite(1); d250;
  }
}
```

### Bidirectional Animation (১→৬→১):

```cpp
void rollDice(int times) {
  for (int i = 0; i < times; i++) {
    // Forward: ১→৬
    for(int j=1; j<=6; j++) {
      LEDwrite(j); d250;
    }
    // Backward: ৫→১
    for(int j=5; j>=1; j--) {
      LEDwrite(j); d250;
    }
  }
}
```

---

## 🐛 সমস্যা সমাধান

### সাধারণ সমস্যা ও সমাধান:

| সমস্যা | সম্ভাব্য কারণ | সমাধান |
|--------|--------------|---------|
| **Display জ্বলছে না** | Power নেই | VCC/GND সংযোগ check করো |
| | 74HC595 power পাচ্ছে না | Pin 16=VCC, Pin 8=GND verify করো |
| | ভুল display type | Common Cathode verify করো (CA নয়) |
| **ভুল segment জ্বলছে** | ভুল wiring | QA→a, QB→b mapping check করো |
| | Pattern inverted | `#define segDISPLAY` toggle করো |
| **Button কাজ করছে না** | Pull-down resistor নেই | 10kΩ GND এ যুক্ত করো |
| | ভুল pin | PB0 (Pin 5) সংযোগ verify করো |
| **Display ঝাপসা** | Resistor বেশি বড় | 220Ω ব্যবহার করো (330Ω এর বদলে) |
| | Low power supply | 5V ব্যবহার করো (3V এর বদলে) |
| **সবসময় একই number** | Random কাজ করছে না | PB5 floating আছে কিনা check করো |
| | Random seed issue | Code আবার upload করো |
| **Flickering** | Wire ঢিলে | সব সংযোগ শক্ত করো |
| | Power অস্থির | 0.1µF capacitor যোগ করো |

### ডায়াগনস্টিক কোড:

```cpp
// সব digit ০-৬ একে একে test করো
void testDisplay() {
  for(int i=0; i<=6; i++) {
    LEDwrite(i);
    delay(1000);  // প্রতিটা digit ১ সেকেন্ড
  }
}

// setup() এ call করো:
testDisplay();

// প্রত্যাশিত: ০→১→২→৩→৪→৫→৬ দেখাবে
```

### বোতাম Test:

```cpp
void loop() {
  int btn = digitalRead(buttonPin);
  if (btn == HIGH) {
    LEDwrite(8);  // চাপলে "8" দেখাবে
  } else {
    LEDwrite(7);  // না চাপলে blank
  }
}
```

---

## 🚀 প্রয়োগক্ষেত্র (Applications)

### ১. বোর্ড গেমে পাশা:

```
লুডু, মনোপলি, সাপ-সিঁড়ি ইত্যাদি গেমে
সাধারণ পাশার বদলে এটা ব্যবহার করা যাবে!
```

### ২. সিদ্ধান্ত নেওয়ার যন্ত্র:

```
৬টা অপশনে confused?
প্রতিটা option কে ১-৬ নাম্বার দাও
বোতাম চাপো → random একটা select হবে!
```

### ৩. লটারি নাম্বার:

```
Code modify করে ০-৯ দেখানো যাবে
লটারি নাম্বার pick করতে পারবে!
```

### ৪. শিক্ষার উপকরণ:

```
শেখানো যাবে:
  • Shift register
  • Random number generation
  • 7-segment display
  • Embedded systems
```

### ৫. Multiple Dice:

```
দুইটা 74HC595 + display যোগ করো:
  • একসাথে দুটো পাশা roll করবে
  • যোগফল দেখাবে
  • Craps game বানাও!
```

---

## 📚 নিজে চেষ্টা করো

### Challenge 1: দ্বিতীয় পাশা যোগ করো

```cpp
// দুটো 74HC595 cascade করো
// পাশাপাশি দুটো digit দেখাও
// দুটোর যোগফল দেখাও
```

### Challenge 2: Sound Effect যোগ করো

```cpp
// Piezo buzzer যোগ করো
// Roll animation এ tone বাজাও
// Final result এ different tone
```

### Challenge 3: Score Memory

```cpp
// EEPROM এ শেষ ১০টা roll save করো
// Button long-press এ stats দেখাও
```

### Challenge 4: Speed Adjust

```cpp
// Potentiometer যোগ করো
// Animation speed control করো (100ms-1s)
// দ্রুত = exciting, ধীর = dramatic
```

### Challenge 5: LED Indicator

```cpp
// ৬টা LED যোগ করো (প্রতি value এর জন্য ১টা)
// Corresponding LED জ্বালাও
// Winning number (৬) = আলাদা রঙ
```

---

## ✨ শিক্ষা (Learning Outcomes)

### যা শিখবে:

```
✅ 7-segment display interfacing
✅ 74HC595 shift register control
✅ Serial-to-parallel conversion
✅ ATtiny85 GPIO programming
✅ Button input with debouncing
✅ Random number generation
✅ Animation and timing control
✅ Binary number representation
✅ Low-power embedded design
✅ Pin-efficient circuit design
```

### Advanced Concepts:

- **Shift Registers**: Serial data transmission দিয়ে pin কমানো
- **Multiplexing**: কম pin দিয়ে বেশি output control
- **Entropy Sources**: Analog noise দিয়ে true randomness
- **Display Encoding**: 7-segment এর জন্য binary pattern
- **State Machines**: Button press sequence trigger করে

---

## 🎯 Success Tips (সফলতার টিপস)

```
১. 74HC595 প্রথমে test করো
   → LED দিয়ে simple test করো shift register

২. Display type check করো
   → Common Cathode নাকি Common Anode

৩. Wiring verify করো
   → QA→a, QB→b, QC→c সঠিক আছে কিনা

৪. Fresh battery ব্যবহার করো
   → 5V recommended উজ্জ্বল display এর জন্য

৫. Pull-down resistor যোগ করো
   → 10kΩ button এ false trigger আটকাবে

৬. PB5 pin floating রাখো
   → A1 connect করো না (true randomness এর জন্য)

৭. Breadboard secure করো
   → Loose shift register সংযোগ সমস্যা তৈরি করে
```

---

## 🎓 প্রজেক্ট সম্পূর্ণ করার ধাপ

### ধাপ ১: Components জোগাড় করো

```
✓ ATtiny85 (1টা)
✓ 74HC595 (1টা)
✓ 7-segment display - Common Cathode (1টা)
✓ Push button (1টা)
✓ Resistors: 330Ω (8টা), 10kΩ (1টা)
✓ Breadboard ও jumper wires
```

### ধাপ ২: Breadboard এ Circuit তৈরি করো

```
১. ATtiny85 ও 74HC595 breadboard এ বসাও
২. Power connections (VCC/GND) সব IC তে দাও
৩. 74HC595 এর control pins যুক্ত করো (SH_CP, ST_CP, DS)
৪. 74HC595 এর OE → GND, MR → VCC
৫. QA-QG outputs resistors দিয়ে 7-segment এ যোগ করো
৬. Common cathode PB1 এর সাথে যোগ করো
৭. Button PB0 এ যোগ করো (pull-down সহ)
```

### ধাপ ৩: Code Upload করো

```
১. Arduino IDE তে code লেখো/copy করো
২. Board: ATtiny85 select করো
৩. Programmer: USBtinyISP (অথবা Arduino as ISP)
৪. Upload করো!
```

### ধাপ ৪: Test করো

```
১. Power ON করো
২. Welcome animation দেখবে (all segments → 0)
৩. Button চাপো
৪. Roll animation দেখবে
৫. Random number (1-6) দেখবে
```

### ধাপ ৫: Troubleshoot (যদি কাজ না করে)

```
→ সব সংযোগ আবার check করো
→ Display type verify করো (CC/CA)
→ Code এ #define segDISPLAY থাকা উচিত
→ Diagnostic code চালাও (testDisplay)
```

---

## 🎉 শেষ কথা

এই প্রজেক্ট দিয়ে তুমি শিখলে:
- কীভাবে shift register ব্যবহার করে pin বাঁচানো যায়
- 7-segment display control করার পদ্ধতি
- Random number generation
- Embedded systems এর মূল ধারণা

এখন তুমি নিজের মতো করে প্রজেক্ট modify করতে পারবে! 🚀

**শুভকামনা তোমার ইলেকট্রনিক পাশা বানানোর জন্য! 🎲✨🎉**

---

## 👨‍💻 Author

**Md. Akhinoor Islam**  
📚 Energy Science and Engineering (ESE)  
🏫 Khulna University of Engineering & Technology (KUET)

---

## 📄 License

MIT License - Free to use, modify, and distribute!

---

**Happy Making! 🎲🔥**
